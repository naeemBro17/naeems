====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-1-functionality
====================================================================

This session worked through "Batch 1 — Functionality Fixes + Card Cleanup +
Design Authority" (10 parts). Everything below is written for a non-technical
reader — see the code comments for the technical detail.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Promo code usage limit (the money bug)
I checked the whole path end to end before changing anything, as instructed.
The validation logic, the database function that increments times_used, and
the page that calls it were ALL already written correctly:
  - Applying a code checks active + max_uses/times_used + expiry (lib/promoCodes.ts).
  - migration-014-promo-codes.sql already defines increment_promo_usage() as a
    SECURITY DEFINER function that atomically re-checks and increments —
    exactly what the task asked for.
  - OrderSuccessPage already calls it exactly once (a ref guards against
    double-firing) when the success screen mounts.
So the code was not the bug. The far more likely explanation, per the
project's own known failure pattern, is that migration-014 (or the specific
function inside it) was never actually run against the live database — a
missing RPC function fails silently, which looks exactly like "the limit
isn't enforced." I hardened the one weak spot I found: the call to
increment_promo_usage was pure fire-and-forget with no error visibility at
all, so a missing function would fail completely silently forever. It now
logs to the console on failure (still doesn't change what the customer sees,
per the existing design — the discount already shown is honoured either way).
ACTION NEEDED FROM YOU: see the SQL section below — please re-run
migration-014 and then do the test the task described (max_uses = 1, order
once, try a second time) to confirm.

PART 2 — Back button from product edit goes to Home
Root cause found: every sheet/modal in the app (the product edit form, the
inline product editor, confirm dialogs, the hamburger menu, etc.) is pure
on-screen state — it never told the browser "something opened." So pressing
the phone's back button had nothing to close; it skipped straight past the
sheet to whatever page was open before you ever opened /admin (often Home).
Fixed with one small shared hook (src/hooks/useModalBackClose.ts) wired into
the two components every sheet/modal in the app is built from
(components/shared/Modal.tsx and BottomSheet.tsx). Opening a sheet now
registers one invisible "back stop" with the browser; back closes the sheet
instead of leaving the page, and closing any other way (Cancel, tapping
outside, Escape) cleans that stop up automatically so you never need an extra
back-press afterwards. Because this lives in the two shared building blocks,
it fixes the bug everywhere at once — not just the product editor — without
touching each sheet individually.
Filter/search/scroll state was already preserved (the admin list never
actually unmounts when a sheet opens), so nothing else was needed there.
The product detail page uses real page navigation (not a sheet) and already
had correct back + scroll-restore behaviour — verified, left alone.

PART 3 — No way back to Home from checkout
Cart, Delivery Details and Order Summary already had a back arrow in the
header (BackButton, already wired correctly, and cart contents are never
cleared just by navigating away). The one real gap was the final success
screen — it had no way out at all besides two action buttons. Added a
"Continue Shopping" button back to the product grid, right below the
WhatsApp/PDF actions, per the spec.

PART 4 — Expert card blocks page scrolling
Found the exact bug: the shared swipe hook (hooks/useSwipe.ts), used by the
bottom-right carousel tile (the expert card + any custom tiles) and the
featured-products stack tile, called preventDefault() on every touchmove the
instant a finger touched it — before it even knew whether the gesture was
horizontal or vertical. That's the exact anti-pattern flagged in CLAUDE.md.
Rewrote it to be direction-aware: it now waits until movement is clearly
along its own axis (more than ~10px and clearly more than the other axis)
before capturing the gesture at all. A finger that lands on the expert card
and moves vertically now scrolls the page normally; only a genuine
horizontal swipe is captured. Also changed the carousel tile's CSS from
touch-action: none to touch-action: pan-y, so the browser handles vertical
panning natively as a second line of defence, per CLAUDE.md's rule.
Checked every other horizontally-swipeable element on the site: the product
image carousel and the hero banner both use plain native horizontal scroll
(no JS touch capture at all), and the category rows (Browse circles, chips)
do too — none of them had this bug.

PART 5 — "Added to cart" toast removed
Deleted the showToast('Added to cart') call from the cart button (the only
place it fired). The card's checkmark animation and the cart badge already
give sufficient feedback, per the task. The toast system itself is used
elsewhere for real errors and promo/order messages, so it was left in place
— only this one call was removed.

PART 6 — Admin/Wholesaler links in the hamburger menu
Added an "Admin Panel" row (admins only, links to /admin) and a "Wholesale
Account" row (approved wholesalers only, links to /wholesaler-access) to the
hamburger menu, styled to match the existing rows. Both are gated on the real
session role from AuthContext (the same source of truth used everywhere
else) — never CSS, never guesswork. Anonymous visitors see neither.

PART 7 — Product variants + skin type/condition fields
Important finding: this was already much further along than the task
assumed. A previous session had already built:
  - The product_variants table + secure view (migration-013, already in the repo).
  - A full admin Variant editor (add/edit/delete Region+Size rows with their
    own prices and stock) — components/admin/edit-sheets/VariantEditor.tsx.
  - Skin Type / Skin Condition multi-select chips in an admin editor.
  - A Region → Size picker on the product detail page that already updates
    price/stock and defaults sensibly for products with no variants.
The catch: all of that only existed in the editor you reach from the live
site's "Edit Mode" (the "..." menu on a product card, admin only) — NOT in
the editor behind the main /admin panel's Products tab, which is what you
actually use day to day. That second, older editor (ProductForm.tsx) had
none of it. That is almost certainly why it looked like nothing had been
built. I did not rebuild anything that already existed — I pulled the same
Variant editor and the same Skin Type/Condition chips into the /admin panel's
product editor too (sharing the exact same components, so there's only one
implementation to maintain), so both places now offer the same functionality.
Also completed the actual buying side of variants, which was still missing:
  - Cart lines now remember which variant was chosen (not just the product).
  - The product detail page now has a proper "Add to Cart" button (there
    wasn't one before — only "Copy Price"). Selecting a Region/Size and
    adding to cart adds that specific variant; a product with no variants
    behaves exactly as before.
  - Cart, Order Summary, the WhatsApp message and the PDF all now show the
    chosen variant (e.g. "AU · 340g") next to the item.
  - Tapping a card's cart button for a product WITH variants now opens the
    product page to choose one, instead of silently guessing the cheapest
    option — the old behaviour would have quietly added the wrong item.

PART 8 — Product card cleanup
  - Removed the category tag pill and the brand line from the card face.
  - Shrunk the cart button (42px → 34px) and its slot, freeing horizontal
    room next to the price/save-badge row.
  - Product name already had 2-line clamping with reserved height in the
    code (min-height for 2 lines) — this looked correct as written; freeing
    the extra room from the removed elements should resolve the truncation
    you saw. Please double check this one on a real phone since I could not
    load the live app this session (see section 3).
  - Save badge + strikethrough already sit on one flex row with no wrapping
    enabled; the extra room from the above changes gives them space to fit.

PART 9 — Product detail page
  9A. Verified: the SKU is never rendered anywhere on the public detail page
      (checked the whole file) — it's used only internally to resolve the
      /product/:slug route. Nothing to fix here.
  9B. The image now runs full-bleed edge-to-edge again. It was inset with a
      margin plus a 20px rounded corner (a deliberate earlier redesign,
      per its own code comment) — reverted just that, without touching the
      shared "text content" padding used by the rest of the page (and by
      the checkout pages, which reuse the same class).
  9C. Replaced the card→detail slide with a shared-element ("hero") transition:
      the tapped card's image box now visually expands into the detail
      page's image box, while the rest of the page fades and lifts in. This
      uses the browser's native View Transitions API (the modern, correct
      way to do this — no hand-rolled animation coordination between two
      separate pages). It automatically falls back to an instant, unanimated
      change on browsers that don't support it yet and whenever the reader
      has "reduce motion" turned on, exactly as the task allows. I could not
      test this live in a real browser this session (see section 3) — this
      is the single change I'd most want you to eyeball on your phone before
      trusting it completely.

PART 10 — Checkout visual redesign
Went through CLAUDE.md's colour rule screen by screen. The checkout flow was
using the brand orange in far more places than "one action per screen":
progress bar fill, every price, the delivery zone's selected state, the
promo "Apply" button, and the success checkmark badge — on top of the actual
primary buttons. Fixed all of it:
  - Progress bar: now fills in near-black, not orange (progress bars were
    explicitly called out in CLAUDE.md as one of the overused spots).
  - Cart line totals and the delivery zone's price: near-black and bold, per
    "prices are near-black, not orange."
  - Selected delivery zone: neutral border/background plus a small check
    icon, instead of an orange border — so "Save and continue" is the only
    orange element on that screen.
  - Order Summary's promo "Apply" button: neutral, so "Pay and Order" is the
    only orange element on that screen.
  - Success screen's checkmark badge: success green (semantically correct
    for a checkmark) instead of orange, so "Send on WhatsApp" is the only
    orange element there.
  - The dark "receipt" summary card was hardcoded to one fixed dark colour
    regardless of light/dark mode — in dark mode it barely showed up against
    the page's true-black background. It now uses its own small set of
    tokens that shift to a slightly lighter charcoal in dark mode, so the
    card stays visibly a card in both themes.
  - General polish pass: tidier spacing/rhythm in the cart list, a rounded
    top edge on the sticky checkout bar, slightly larger/bolder totals for
    clearer hierarchy, consistent thumbnail borders.
Nothing about the functional flow, routes, delivery prices, promo logic, PDF
export or WhatsApp send was touched — only how these four screens look.

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
No NEW migration files were created this session — everything Part 7 needed
(product_variants, skin_types/skin_conditions) already existed as migration
files in the repo. But per the project's #1 recurring failure mode, please
verify these have actually been run in the Supabase SQL Editor (all are
explicitly safe to re-run — each says so in its own header):

  1. supabase/migration-012-skin-fields.sql   (skin_types/skin_conditions columns)
  2. supabase/migration-013-variants.sql      (product_variants table + view)
  3. supabase/migration-014-promo-codes.sql   (THIS IS THE LIKELY FIX FOR PART 1
                                                — it creates increment_promo_usage())

Run them in that numeric order if you're not sure which have been applied.
No other SQL is needed for anything else in this session.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------
This environment has no Supabase credentials (.env only has the example
file), so the app cannot actually load real data here — I confirmed it fails
immediately with "Missing Supabase configuration" in the browser console.
That means I could build and type-check everything (npm run build passes
with zero errors, confirmed twice), but I could NOT visually test any of
this in a real browser or on a phone this session, per CLAUDE.md's own rule
that I should say so plainly rather than claim it works.

Please specifically eyeball, on a real phone, before fully trusting them:
  - The new card → detail hero transition (Part 9C) — the riskiest, newest
    piece of code this session.
  - The product card layout at real narrow width (Part 8) — two columns,
    ~160-180px each.
  - The expert card / bento carousel still scrolling correctly (Part 4).
  - The variant picker + Add to Cart on the product detail page, end to end
    into the cart and WhatsApp message (Part 7).
  - The Part 1 promo code fix, after you've re-run migration-014.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- There are two separate product editors in the codebase: the one behind
  /admin → Products (ProductForm.tsx) and a more modern one reached from
  the live grid's "Edit Mode" (ProductEditSheet.tsx). They now both have
  variants and skin fields, but they're still two separate forms with some
  duplicated fields (e.g. images, description). Worth consolidating into one
  editor in a future session — right now it's possible to update a product
  in one and not notice the other exists.
- CartContext's cart items are stored in localStorage with no expiry and no
  price re-validation against the current catalog until checkout. Not a bug,
  just worth knowing: a cart sitting untouched for a long time could carry a
  stale price snapshot until the customer reaches checkout, where the real
  product is re-resolved anyway.
- .env is genuinely absent from this environment (only .env.example exists),
  which is why nothing could be visually verified — flagging in case that's
  unexpected on your end too.

--------------------------------------------------------------------
5. DESIGN DECISIONS (where I had freedom)
--------------------------------------------------------------------
- Part 7 cart-button-on-card behaviour for variant products: rather than
  silently adding the cheapest variant (which could ship the wrong size/
  region), tapping the cart button on a card for a product WITH variants now
  opens the product page so the customer picks one. This matches how the
  card already shows a "from" price for such products.
- Part 9C: chose the browser's native View Transitions API over a hand-built
  FLIP animation. It's the standard, well-supported modern approach for
  exactly this "shared element" pattern, needs far less custom coordination
  code, and comes with an automatic, correct instant-fallback for
  unsupported browsers and reduced-motion users built in — which is exactly
  what CLAUDE.md's fallback clause asks for.
- Part 10's "receipt" dark card on Order Summary: kept the existing dark
  overlay-card look (it's a common, credible pattern for a final order
  review) rather than converting it to a plain light card, but made it
  properly theme-aware since it wasn't before.
- Part 8: chose to fully remove the category badge / brand line from the
  card (not just hide them conditionally) since CLAUDE.md's card spec
  explicitly lists both as "not on the card."

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------
Highest impact first.

1. [Conversion / trust — medium] No order records anywhere. As CLAUDE.md
   already notes, an order today only exists as a WhatsApp message and a
   PDF — if a customer's message gets lost, or WhatsApp misbehaves, there is
   no record on your side at all. An orders/order_items table with price
   snapshots (already flagged as the next major planned feature) would also
   let you eventually show "order history" and would make the promo code
   usage count auditable against real orders instead of trusted blindly.

2. [Code health — medium] Two parallel product editors (see section 4).
   Confusing to maintain, and a real risk that a field gets added to one and
   forgotten in the other (skin fields being the field that WAS forgotten,
   which is exactly why this session existed).

3. [Conversion — small/medium] Site-wide orange overuse, per CLAUDE.md's own
   audit, still exists everywhere OUTSIDE the checkout flow — product card
   prices, save badges, stock dots, nav tabs are still orange across the
   rest of the site. This session deliberately did not touch that (out of
   scope per your own instructions), but it's the same issue Part 10 just
   fixed for checkout, applied everywhere else.

4. [Accessibility — small] Several icon-only buttons (cart button, card
   menu trigger) already have aria-labels, which is good — but the new
   variant chip selectors (Region/Size) rely on colour + a filled pill for
   "selected" state with no additional text cue for screen readers beyond
   aria-pressed. Low priority, already functional, just not best-in-class.

5. [Performance — small] The production build reports one JS chunk over
   500KB (mostly jsPDF + html2canvas, pulled in for the PDF export). It's
   already lazy-loaded (only downloaded when someone taps "Save as PDF"),
   so this isn't hurting first load — just flagging since the build tool
   warns about it.

6. [Data model — small] Cart items persist forever in localStorage with no
   cap or expiry. Not urgent at ~150 SKUs, but worth a note for later.

====================================================================


====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-2-card-and-transition
====================================================================

This session worked through "Batch 2 — Instant Product Open + Product Card
Layout" (3 parts). The .env file now has real credentials, so — unlike last
session — everything below was actually run and looked at in a browser, not
just read in the code. Where I say "verified," I mean I saw it on screen.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Instant product open (the 2-second delay)

Found the real cause, and it was not what the task guessed. The theory was
that the detail page waits on a Supabase fetch before it can paint. I checked:
when you tap a card from the grid, that exact product is already sitting in
the app's in-memory catalog (the same list that rendered the card), so the
detail page was already resolving it instantly with no network call — that
part was never actually the bottleneck.

The real cause: last session's hero transition (the card image morphing into
the detail page image) tagged EVERY product card in the grid — all ~100+ of
them — with the marker the browser uses to know what to animate, not just the
one being tapped. Every single tap, the browser had to prepare an individual
animation snapshot for all ~100+ real product photos before it could show
anything, even though only one of them was actually going to move. That is a
well-known, expensive mistake with this browser feature, and it would be far
worse on a phone's GPU than in a desktop browser — matching exactly the "taps
and stares at the old screen" symptom.

Fixed it at the source: only the card actually tapped gets tagged now, set at
the instant of the tap rather than in advance on every card. Confirmed by
directly counting tagged elements before and after a tap: 105 before this fix,
1 after. Confirmed by clicking a real card in the running app: the URL and the
new page both update within the same tick, no visible delay.

On top of that fix, I also built what the task asked for directly, as
genuine extra insurance (and because a couple of its individual requirements
were real, even though the "waiting on a fetch" theory wasn't the main
story):
  - The exact product row a tapped card was already showing is now handed to
    the detail page through the navigation itself (router state), so the
    very first render has real data to paint from no matter what — it
    doesn't have to depend on the catalog context noticing in time.
  - The transition is never awaited on anything — it starts in the same
    click handler that changes the page, before any network activity.
  - Opening the same product again is already instant and always was — nothing
    is re-fetched, since the catalog is loaded once and kept in memory for the
    whole visit.
  - Direct/shared-link opens (no card to inherit data from) still go through
    the original fetch-then-render path — but that full-page spinner has been
    replaced with a shaped skeleton (an image-sized block, name-sized bars, a
    price-sized bar) that matches the real layout, so it doesn't jump around
    once the real content arrives.

One thing genuinely does not apply to this app and I did not force it in: the
task asks for "the top paints instantly, the bottom fills in afterward with
its own skeleton." This app fetches a product's entire row in one query —
image, name, price AND description, how-to-use, ingredients, video, variants
— there is no second, slower fetch for the lower half of the page to begin
with. Building a fake split-loading state for data that already arrives
together would have added real complexity for no actual benefit, so I didn't.

Also found and fixed a real bug while looking at the rendered detail page for
the first time: the "Copy Price" button was still rendering orange (it was
supposed to become the neutral secondary action once "Add to Cart" became
the primary one, from last session's checkout-colour work). The CSS rule that
made it neutral existed, but was declared BEFORE the button's base style in
the stylesheet, so the base orange was winning. Moved it after. Confirmed on
screen: Add to Cart is now the only orange element on the page, Copy Price is
neutral grey, and the green "Copied!" flash still works on both states.

PART 2 — Product card layout

Verified on a REAL narrow viewport this time (see section 3 below for exactly
how, since the standard browser tools here can't shrink below ~650px). Tested
at 320px, 360px, 375px and 390px — the full realistic phone range, not just
the 160-180px the task asked for.

What I actually found, measured directly against every rendered card rather
than guessed:
  - The price-wrapping bug (Problem B) and the badge/cart-button collision
    (Problem C) did NOT reproduce at 375-390px (a modern phone) with the code
    as merged from last session — but they reproduced on EVERY single card
    once the viewport dropped to 320px (an older/smaller phone), which is
    still a real device width. So the bug was real, just narrower than where
    it was originally seen — plausibly the live phone tested against had a
    slightly narrower effective width, or different font metrics, than my
    360-390px checks alone would have caught.
  - Fixed it at the root instead of chasing the exact width: shortened the
    save badge from "Save 12%" to "-12%" (same idea the task offered as the
    fallback) on every card, consistently, and added `white-space: nowrap`
    to the price, the struck-through retail price and the badge so none of
    them can ever break internally again, at any width. Also shrank the cart
    button a little further (30px, then 28px) exactly as the task allowed,
    to free more room.
  - Re-measured after the fix at all four widths: zero cards wrap at any of
    them, and the gap between the badge and the cart button is never below
    7px even at the extreme 320px width (it's 35-42px at realistic
    375-390px widths). No collisions found anywhere in the ~105-product
    catalogue.
  - Problem A (names truncating early) is real but only partly fixable with
    CSS alone. I confirmed exactly why: with a long name like "CeraVe Foaming
    Cleanser Eco-Recharge Refill 473mL" at card width, the word "Recharge"
    (8 letters) simply doesn't fit in what's left of the second line after
    "Cleanser Eco-", so it wraps whole to a third line that then gets cut —
    leaving a visible gap on line 2 before the ellipsis. I tried both
    automatic hyphenation (`hyphens: auto`) and character-level breaking
    (`word-break`) directly in the running browser to see if either would
    tighten that gap — neither changed the outcome, because browsers only
    break inside a word when the word cannot fit ANYWHERE on its own, never
    just to pack a line more tightly. That is a hard limit of how text
    wrapping works, not a bug in this app, and the task explicitly ruled out
    the other usual fix (shrinking the font). What I could do — and did — is
    free up real width by shrinking the cart button further, which reduces
    how often this happens without touching font size. A handful of the
    longest names in the catalogue will still show a modest gap on their
    second line; renaming those specific products to something shorter is
    the only way to fully close that gap, and that's a content decision, not
    a code one.
  - Checked every required case by hand at 375px: longest name (Aveeno Baby
    Daily Moisture Fragrance Free Lotion), long name with a hyphenated word
    (CeraVe Foaming Cleanser Eco-Recharge), medium name (Beauty of Joseon Red
    Bean Water Gel), short out-of-stock name (Bio Oil | 200mL — confirmed its
    save badge correctly disappears because it's out of stock, and the cart
    button shows disabled), and the largest discount badges in the catalogue
    (all "-XX%" now, same width regardless of the digits). Also confirmed a
    genuinely no-discount product shows just its price with no leftover gap.

PART 3 — Verifying two things from last session

3A. SKU hidden from customers — CONFIRMED, still correct. I did not just
    re-read the code this time: I loaded a real product page as a signed-out
    visitor and searched the entire rendered page (every accordion section,
    and the full HTML including anything hidden) for anything matching the
    SKU pattern this app generates (e.g. "FC-0366"). Nothing found anywhere.
    Nothing to fix.

3B. Product image edge-to-edge — CONFIRMED, correct, saw it directly on
    screen at phone width: the image touches both the left and right edges
    with no visible gap and no rounded corner cutting into it, and the text
    below it still has its normal padding. No change needed.

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
None needed. Nothing in this session touched the database or its schema.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------
Nothing outstanding from the three parts asked for. One honest limitation in
how I verified Part 2: the browser tooling available in this environment
would not resize down to a true 375px window (it kept reporting ~650-770px
wide no matter what I asked for — some kind of minimum-window-size floor).
I worked around it by loading the real running site inside a same-origin
iframe pinned to the exact widths I needed (320/360/375/390px) and measuring
every card's actual rendered dimensions in the real browser engine, which is
why I'm confident in the specific pixel numbers reported above — but it's
still worth you double-checking on your own phone, since nothing beats the
real device. If anything looks off there that doesn't match what's described
above, that's the first place to look.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- The "Copy Price is still orange" bug described in Part 1 above is the kind
  of thing that will keep happening at the edges of the checkout/detail
  colour work from two sessions ago, precisely because that work was never
  seen running until today. It's worth a quick visual pass over the rest of
  last session's colour changes (dark mode especially) next time you're in
  the app, since I only spot-checked the pages this batch's tasks touched.
- The product grid holds ~105 products in memory at once, all rendered
  simultaneously (no virtualization/windowing). That's fine at this catalogue
  size and is not slowing anything down today, but it's the kind of thing
  that would start to matter if the catalogue grows substantially — noting
  it now rather than after it becomes a problem.

--------------------------------------------------------------------
5. DESIGN DECISIONS
--------------------------------------------------------------------
- Save badge format ("-12%" instead of "Save 12%"): applied to the product
  card only, not the product detail page or the homepage bento tiles, which
  have real room to spare and where the fuller wording reads better. The task
  asked for one consistent format "for all cards," which I read as the grid
  card specifically, not every place a discount appears anywhere in the app.
- Cart button size: shrank it twice this session (34px → 30px → 28px) chasing
  the narrowest realistic width before any collision appeared. It's now
  smaller than a typical comfortable tap target, which the task explicitly
  accepted as a trade-off ("it is already only an icon") — worth knowing the
  whole card is still tappable as a fallback if a thumb narrowly misses it.
- Kept the router-state data-passing and skeleton work from Part 1's brief
  even though my own measurement shows the grid-tap path didn't strictly need
  it for speed (the catalog context already had the data instantly). It's
  cheap, it's exactly what was asked for, and it's genuine insurance against
  edge cases (a momentarily-stale context, a future change to how products
  load) — but I want you to know the honest reason it was built, not just
  that it was.

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------
1. [Code health — small] Now that CSS bugs are only ever going to be caught
   by actually looking at the running app, it may be worth asking whichever
   session next touches the visual side of the site to do a full click-
   through of every screen (not just the ones that changed) once, the way
   this session did for checkout — a cheap way to catch the "looked right in
   the code, wrong on screen" class of bug before it ships.
2. [Content — small] A handful of product names are long enough that even a
   well-tuned two-line card can't show them in full (see Problem A above).
   Not a bug to fix in code — worth a look at whether a few of the longest
   names (there are maybe half a dozen) could be shortened in the catalogue
   itself for a cleaner card, e.g. dropping a redundant size/variant suffix
   already shown elsewhere.

====================================================================


====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-3-card-and-images
====================================================================

This session worked through "Batch 3 — Card Text, Save Badge, Banner and
Category Images" (4 parts). Parts 1 and 2 were verified by eye in a real
running browser at 320/360/390px, same as last session. Part 3 was also
verified by eye. Part 4's customer-facing side was verified by eye; its
admin side could not be — see section 3.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Card names wasting the first line

Confirmed the bug exactly as described, at 375-390px: "Aveeno Daily" filled
maybe two-thirds of line 1 and then jumped "Moisturising" whole to line 2,
leaving real empty space behind. Measured it precisely before touching
anything: the word "Moisturising" (12 letters) simply didn't fit in what was
left of line 1, so the browser moved the entire word down rather than
splitting it — normal, correct CSS text-wrapping behaviour, just not the
behaviour this card needs.

The task suggested `overflow-wrap: anywhere` as the fix. I tried it, then
checked with an isolated test in the real browser engine whether it actually
changed anything — it did not. I want to be precise about why, since it
matters for any future styling decision like this: `overflow-wrap` (in
either its `break-word` or `anywhere` form) only ever breaks a word that
doesn't fit ANYWHERE — i.e. is wider than the whole line box on its own. It
does not break a word that merely doesn't fit in what's left of a
partially-filled line but WOULD fit comfortably starting fresh on the next
line — and "Moisturising" easily fits on its own line at this card width, so
neither property touched it. I verified this with a direct side-by-side
comparison of "normal" wrapping vs. "anywhere" on the exact failing text at
the exact failing width — the line breaks were byte-for-byte identical.

The property that actually does what's needed is `word-break: break-all`,
which allows a break at any character boundary regardless of whether the
word would fit on a fresh line. Applied it, then looked at the real result
on every name the task asked me to check:
  - "Aveeno Daily Mois / turising Lightly S…" — both lines now fully used.
  - "Aveeno Oat Milk B / lend Moisturisin…" — both lines fully used, but the
    break lands after a single letter ("B"), which does look like a stray
    character rather than an intentional break.
  - "Beauty of Joseon / Relief Sun Aqua-F…" — breaks fall at natural word
    boundaries here regardless, so this one looks completely normal.
  - "CeraVe Foaming / Cleanser Eco-Rec…" — fills further than before
    (previously broke after "Eco-", now after "Eco-Rec"), reads fine.
  - "Bio Oil | 200mL" — short name, unaffected, still reserves the full
    two-line height so price rows stay aligned with longer-named neighbours.
I also tried combining this with automatic hyphenation (`hyphens: auto`) to
see if the browser would insert a proper hyphen at a real syllable break
instead of an arbitrary character split — it made no difference; break-all's
break opportunities override hyphenation's the same way they override normal
word boundaries.

Net result: the wasted-line bug the task rejected is gone on every test
name, which is what was required. The genuinely honest trade-off, per the
task's own instruction to report rather than silently revert: a small number
of long names will occasionally show a bare mid-word split with no hyphen
mark, and in the "Aveeno Oat Milk Blend" case that split happens to land
after a single orphan letter, which is the roughest-looking result I found
across the whole catalogue. I judged this an acceptable trade for "every
card actually uses the room it has," per the task's explicit instruction,
but it's a real, visible characteristic of this choice, not a non-issue —
see section 6 below for the actual long-term fix (shortening a few names).
Font size was not touched anywhere in this change.

PART 2 — "Save X%" wording restored

Changed the badge text back from "-12%" to "Save 12%". Tested immediately
afterward at the widths the task named:
  - 390px: comfortable, 35px+ clear of the cart button on every card.
  - 360px: comfortable, 19px+ clear.
  - 320px: at the original badge size this collided — badges were
    overlapping the cart button by up to 13px on some cards. Reduced the
    badge's font size (8.5px → 7px) and padding, per the task's explicit
    fallback instruction ("reduce the badge's font size and padding
    slightly rather than dropping the word"). Re-tested: 0 wrapping cards,
    minimum 4px clearance from the cart button at 320px, checked against
    every card currently in the catalogue including the largest discounts
    (up to -45%, wording doesn't change badge width since "Save XX%" is a
    fixed 8-character shape regardless of the digits).
The word "Save" is now on every discount badge, unconditionally, at all
three checked widths, with no wrap and no collision anywhere.

PART 3 — Hero banner image support

Built this as: an optional per-slide photo, uploaded through the same
Supabase Storage mechanism product images already use (resize client-side to
WebP, upload, read back a public URL) — no new bucket, no new upload
mechanism, per the task's instruction. With no image, the slide is
byte-for-byte the same markup as before this change (verified on screen —
see below). With one, the photo fills the slide behind a dark scrim, with
the heading, eyebrow and "Shop Now" button on top.

I could not log into the live admin panel to test the upload button itself
(see section 3), so I verified the two things that matter most a different
way — by injecting a real photo from the shop's own Storage bucket into the
actual running page's DOM, using the exact CSS classes the real component
outputs, and looking at the actual rendered result:
  - No image (today's real data): confirmed unchanged — same dark block,
    same decorative circles, same layout, at 390px.
  - With a real photo loaded: my first scrim attempt (a diagonal fade,
    strong at bottom-left, nearly gone at top-right) genuinely failed the
    "readable over any photograph, light or dark" requirement — tested
    against an actual light pink product photo from the catalogue, and the
    tail end of a two-line headline that reached into the faded corner
    lost real contrast. I did not accept that as good enough. Replaced it
    with a vertical gradient that never drops below ~40% black anywhere in
    the slide, strongest at the bottom where the title and button actually
    sit. Re-tested with the same photo: fully legible white heading and
    button across the whole slide, photo still clearly visible. Height,
    rounded corners and the button's orange stayed exactly as before — the
    button is still the one orange element in the banner.

PART 4 — Browse category circle images

Same storage pattern as Part 3, this time per category. Needs one new
database column that does not exist in the live database yet — see the SQL
section below; I could not run it myself (no service-role access from this
environment, and it's exactly the kind of schema change CLAUDE.md says you
run by hand). I built and type-checked the whole feature, and verified the
customer-facing rendering the same DOM-injection way as Part 3:
  - No image (every real category today): confirmed unchanged — same
    curated icon, same circle, at 390px.
  - With a real photo injected into a live circle: crops cleanly to fill the
    circle (cover, centred), no distortion, category name underneath
    untouched. Also specifically checked the circle's "NEW" badge (the small
    red dot some circles carry) doesn't get clipped by the new image now
    that the circle can show a full-bleed photo — it doesn't; the rounding
    lives on the image itself, not the circle wrapper, precisely so that
    badge (which sits slightly outside the circle's own bounds) keeps
    showing over an image exactly as it does today over an icon.
  - Could NOT verify: the actual admin upload flow, or a real photo coming
    from a real category row, because the column doesn't exist in the
    database yet. Please look at this one yourself after running the SQL
    below.

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
One new migration, needed for Part 4 only (Part 3 needed no schema change —
banner slide data is a JSON blob in app_settings, not a table). Safe to
re-run.

File: supabase/migration-015-category-images.sql

  ALTER TABLE categories ADD COLUMN IF NOT EXISTS image_url text;

That's the whole migration — categories has always been a plain "anyone can
read every column" table (see rls.sql), so nothing else (no view, no column
grant) is needed for the new column to become visible, unlike products'
wholesale_price-style columns.

No storage bucket or storage policy SQL is needed — both the banner image
and the category image reuse the existing product-images bucket and its
existing policies, the same way the admin's expert-photo upload already
does.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------
- I do not have admin login credentials in this environment, so I could not
  sign into /admin or turn on Edit Mode to actually click through the
  banner-image and category-image upload buttons I built. I verified their
  code by type-checking and by reading it carefully against the existing
  expert-photo upload it's modelled on (which does work, and which I did not
  change), and I verified the RENDERING side (what a customer sees) by
  injecting real photos into the live running page — but the actual
  admin-facing "pick a file, watch it upload, see it save" flow is unverified
  by eye. Please try it yourself first before trusting it fully — upload a
  banner photo and a category photo, refresh, and confirm both stuck.
- Part 4's admin and customer-facing code for a real (non-injected) category
  photo is entirely unverified until you run migration-015 — right now the
  database simply doesn't have anywhere to put the URL, so saving a category
  with an image chosen would fail with a database error today. This is
  expected and not a bug; it'll work once the column exists.
- Same environment gap as every prior session: no way to actually click
  "Save" as an authenticated admin and watch a network request succeed.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- The banner slide's "Background colour" field, editable in the admin sheet
  for as long as banner slides have existed, is never actually applied
  anywhere in HeroBanner.tsx — the slide always used one fixed CSS colour
  regardless of what was saved there. Not something this batch asked me to
  fix, and I didn't touch it, but it means that field has quietly done
  nothing since it was built. Worth deciding whether to wire it up or remove
  it in a future session.
- Bento tiles (the homepage's admin-managed cards) use a THIRD, different
  image mechanism from everything else — a plain "paste a URL" text field,
  no upload, no Storage bucket at all. Not a bug, just worth knowing there
  are now three different patterns for "give this thing a photo" in the
  codebase (multi-upload for products, paste-a-URL for bento tiles, and now
  the single-photo Storage upload this session generalised for banner/
  category/expert-photo). Consolidating bento tiles onto the same
  Storage-upload pattern would be a reasonable future cleanup, not urgent.
- Banner slides had no stable id before this session — the admin list used
  to key each row on `${title}-${index}`, which breaks (duplicate React
  keys, wrong row edited) if two slides ever share a title or index shifts
  from reordering. Gave every slide a real `id` (generated once, kept for
  its lifetime) as part of adding the image feature, since the image needed
  a stable storage path anyway. This is a small, low-risk correctness fix
  that fell directly out of this session's work, not a separate change.

--------------------------------------------------------------------
5. DESIGN DECISIONS
--------------------------------------------------------------------
- Part 3/4's upload widget is a NEW small shared component
  (AdminImagePicker), not the existing multi-image ImageUploader product
  photos use. I checked first: that component is built specifically for
  "add several photos, reorder them, mark one as cover," which doesn't fit
  "this one thing optionally has exactly one photo." The codebase already
  had a precedent for that exact shape of problem — the admin's expert-photo
  field, which has its own small single-photo upload widget separate from
  ImageUploader. I pulled that existing pattern out into one shared
  component instead of writing it a third time inline in both the banner
  and category editors, and reused the exact same Storage upload mechanics
  (same bucket, same client-side resize) everywhere. I read this as
  satisfying the task's actual instruction — "don't invent a second way of
  uploading images" — since nothing about how or where images are stored
  changed; only the fact that three fields sharing one shape of UI now share
  one implementation of it. I did not touch the existing expert-photo field
  itself to use this new shared component — it already works, and rewriting
  working code to a new shared version was not asked for this session.
- Each banner slide's image is stored at a fixed path keyed to the slide's
  new id (banners/{id}.webp) with upsert, rather than a fresh random
  filename per upload. Replacing a slide's photo overwrites the same file in
  Storage instead of leaving the old upload behind — the same choice the
  existing expert-photo field already makes, for the same reason.
- Category images work the same way, at categories/{category id}.webp —
  using the category's own database id, which is already stable, rather
  than generating a new one.
- The scrim redesign in Part 3 is the one place this session made a real
  judgment call the task explicitly authorised ("judge the strength by
  looking at it, not by guessing a value") — documented in the CSS comment
  directly above the rule, including which photo it was tested against and
  why the first attempt wasn't good enough.

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------
1. [Content — small, carried over from last session] The specific long
   product names that now show a mid-word break with no hyphen (see Part 1)
   would look meaningfully cleaner if shortened slightly in the catalogue —
   "Aveeno Daily Moisturising Lightly Scented Body Wash 354mL" is long
   enough that no purely visual fix avoids some compromise. This is a data
   decision, not a code one, and is the actual permanent fix for Part 1's
   remaining rough edges.
2. [Code health — small] Now that three different "give this one thing a
   photo" implementations exist in slightly different shapes (see section 4
   above), migrating the expert-photo field and the bento-tile URL field
   onto the new shared AdminImagePicker component would leave exactly one
   pattern to maintain instead of three. Low risk, not urgent.
3. [Process — worth remembering] This is the third session in a row where a
   real, previously-unverified problem was only caught once someone actually
   looked at the rendered page rather than reasoning from the code (the
   wasted-line-1 bug in Part 1 is exactly this again, on top of Part 1's own
   finding last session and the Copy Price colour bug two sessions ago).
   Worth keeping "look at it running" as a standing requirement for any
   future visual work, not just when a bug report specifically demands it.

====================================================================


====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-4-banner-variants-admin
====================================================================

This session worked through "Batch 4 — Banner, Cart Button, Variants, Admin
Convenience" (9 parts). Every part below was verified live in the running
app — I logged in as admin (you gave me test credentials at the start of
this session) and clicked through the real UI, not just read the code. The
one exception is noted explicitly in section 3.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Card name still wrapping badly

You were right that "it's fixed" from two sessions ago wasn't true. I did
not guess this time — I measured the real card in the real running app with
the browser's own text-measurement APIs, on the exact product you named
("Aveeno Baby Daily Moisture Fragrance Free Lotion 227g").

The real numbers: at the card's actual rendered width, the name box is
114.4px wide, font is 700 weight / 11.5px / Plus Jakarta Sans. "Aveeno Baby
Daily" measures 103.4px — it fits on line 1 with room to spare. The bug
wasn't this exact name failing to fit; it's that word-break: break-all (last
session's fix) breaks ANY word wherever the line runs out of room, even when
the whole word would have fit cleanly on the next line. I ran this check
across every product name in the catalogue (105 cards) and found real
examples: "Beauty of Joseon Red Bean Water Gel" was breaking "Red" into
"Re" + "d", "Avene Thermal Spring Water 300 ml" was breaking "Spring" into
"Sprin" + "g" — exactly the orphan-letter problem you flagged, just on
different products than the one you happened to screenshot.

The fix: I wrote a small measurement-based line-splitter
(src/lib/textWrap.ts) that runs in JavaScript instead of relying on CSS to
guess. For each card name it: fills line 1 with whole words for as long as
they fit; if the rest of the name then fits whole on line 2, stops there —
no breaking, ever, when normal wrapping already works. Only when the name
would need truncating anyway does it consider spilling part of the next
word onto line 1 to use the space — and only if doing so leaves at least 2
characters on both sides of the break, so a break never strands a single
letter. ProductCard now measures its own rendered width with a
ResizeObserver and renders two pre-computed lines instead of leaning on CSS
word-break at all.

Verified: ran the same orphan check across all 105 visible cards after the
fix — zero orphans. Zoomed into the exact "Aveeno Baby Daily Moisture
Fragrance Free Lotion 227g" card and several others by eye — lines fill
properly, breaks (where they still happen, only on names that were already
being truncated) land on 2+ character boundaries, nothing looks broken.
Font size and the two-line height reservation are both unchanged.

PART 2 — Hero banner: remove text/scrim over photos, keep a medium Shop Now

Removed the heading, eyebrow text and dark scrim from a banner slide's photo
branch entirely — a slide with an image now shows only the photo, full
brightness, edge to edge, plus a "Shop Now" pill button. The no-image
fallback (today's plain default background with the heading/eyebrow/CTA) is
completely untouched — same markup, same styling, verified unchanged on
screen.

The button is sized medium on purpose: 10px/20px padding, 13px bold text —
between the tiny ~28px-tall pill it was and the app's full 44px primary
button, positioned bottom-left with 14px margin so it never touches the
photo's rounded corner.

Banner dimensions (for the helper text and your own reference): the slide
box is a fixed 148px tall regardless of screen width, and exactly
(viewport minus 32px) wide — so 328px at 360px phones, 358px at 390px,
382px at 414px. Because height is fixed but width isn't, the box's aspect
ratio actually shifts slightly across phone sizes (2.22:1 up to 2.58:1);
object-fit cover crops a wider source image's sides on a narrower phone,
never its top or bottom, so nothing important should sit right at the
left/right edges of your photo. Recommended upload size: 1200x460px or
larger, same ~2.6:1 shape — that covers the widest real case (414px-wide
phone) at 3x pixel density without the browser upscaling anything. This
exact text is now in the admin form itself, not just this file — see below.

The admin banner editor now shows this recommendation as helper text
directly under the image upload button (same pattern as the SKU field's
"Generated automatically... Not shown to viewers" note), and the
Eyebrow/Title fields — which would do nothing on a photo slide now — hide
themselves the moment an image is set, reappearing if the image is removed.
I chose to hide rather than grey them out because a photo slide's title is
genuinely inert now, and a form field that visibly does nothing invites
confusion. The "Slide title is required" validation was relaxed to only
apply when there's no image, so this doesn't create a dead end.

Verified on screen at 390px: real photo, no text or scrim drawn over it,
Shop Now button visible and tappable in the bottom-left corner. Opened the
admin editor for both a photo slide (fields hidden, helper text present)
and the empty/no-image slide (fields shown as before) to confirm both paths.

PART 2 — Cart button too small

Restored the cart button from 28px back to 34px (its pre-shrink size), and
bumped the icon inside it from 12px to 15px to match. Rather than shrinking
the button again if the badge got tight, I left the button alone and
re-checked the badge at the narrowest realistic width.

Verified programmatically across every card with a discount (96 cards) at
320px, 360px and 390px real CSS-pixel widths (simulated by resizing the
actual grid container to those exact widths, since this environment's
Chrome window won't shrink its viewport below about 400px — see section 3):
zero collisions at any width, minimum clearance 4.3px at 320px (the
tightest case), 24px at 360px, 39px at 390px. Confirmed visually too — the
button is clearly larger and doesn't touch the "Save 12%" / "Save 20%"
badges anywhere in the catalogue.

PART 4 — Variant delete was closing the whole sheet

Reproduced it exactly as described: opened a product with 3 variants,
deleted the middle one, and the entire "Edit Product" sheet closed and
dropped back to the grid.

Root cause, and it's a real bug in shared code (src/hooks/useModalBackClose.ts),
not anything specific to variants: every sheet/modal in this app pushes one
"fake" browser-history entry while open, so the hardware/browser back button
closes it instead of leaving the page. When it closes normally (Cancel,
Confirm, Save), its cleanup pops that fake entry with history.back() so you
never need an extra back-press later. The bug: that history.back() call
fires a real popstate browser event — and the PARENT sheet (still open,
still listening for its own back button) was mistaking that event for
someone pressing back on IT, and closing itself too. Deleting a variant
opens a small confirm dialog (nested inside the product sheet); confirming
the delete closes that dialog, whose cleanup's history.back() was
accidentally closing the whole product sheet above it.

Fixed by having every closing sheet mark "this next popstate is mine, not a
real back-press" right before it pops its own entry, so a parent sheet
still open at the time can tell the difference. This is a shared hook, so
the fix protects every nested confirm-dialog-inside-a-sheet in the app, not
just variants.

Verified: added 3 test variants to a real product (cleaned up afterward),
deleted the middle one by its exact "Delete {region} {size}" button — sheet
stayed open, scroll position was essentially unchanged (1538px before to
1484px after, matching the row that disappeared), the other two variants
were untouched, and the browser's back-button bookkeeping still worked
correctly afterward (confirmed no extra history entry was left dangling).

PART 5 — Variant image, stock quantity, note + three-state Region/Size

This was the big one. New database columns (migration-016 — you already ran
it this session):
  - product_variants: image_url, stock_quantity, note
  - products: region, size

The three-state behaviour you asked for now works exactly as specified:
  1. No label, no variant rows — unchanged from before, single item, no
     selector, no label text.
  2. A Region and/or Size label set on the product itself, still no variant
     rows — shows as plain text under the name (e.g. "AU 350mL"), no
     selector, product's own price/stock used directly. Verified on the
     real "Beauty of Joseon Red Bean Water Gel" product.
  3. One or more real variant rows added — the product's OWN price, stock,
     image and note become the FIRST selectable option automatically (never
     re-entered), and a real selector shows every option, including the new
     rows. Verified by adding a real "US 473mL" variant to the same
     product: the card correctly switched to "from Tk1,850" (the lower of
     the two prices, correctly considering the product's own price too, not
     just the new variant), the detail page showed both chips, and
     switching between them correctly changed price, stock status, and the
     variant's own note text ("US bigger refill pouch").

Variant image: when a variant has its own photo, it becomes the FIRST image
in the gallery wherever that variant is selected (product detail page,
cart, checkout order summary) — the rest of the product's photos are still
there, just not first. WhatsApp and the PDF export carry no images at all
in this codebase currently (they're text-only) so "variant image in
WhatsApp/PDF" doesn't apply — nothing to change there, it's not a gap.

Variant stock quantity: follows the exact same convention the product
itself already uses (isInStock in src/lib/stockStatus.ts) — a tracked
quantity, when set, always wins over the plain In Stock toggle, and only an
explicit 0 forces Out of Stock. Same "Not tracked" placeholder, same
component pattern, reused rather than reinvented.

Variant note: falls back to the product's own note when the variant doesn't
have one of its own — so most variants (which won't need a special note)
still show the product's existing note by default.

Shared link with a variant selected (Part 4C): selecting a variant updates
the URL to include ?variant=<its id> (a real UUID, not a hand-built slug —
see section 5 for why). Opening that exact URL fresh — I tested this in a
brand-new tab, not just the same tab's back button — pre-selects the right
variant immediately: correct price, correct stock, correct note, right chip
highlighted. A missing or invalid ?variant= value correctly falls back to
the default (the product's own base option) rather than erroring.

PART 6 — Edit in the card's "..." menu

This turned out to already be built and working — the "Edit Product" option
was already in the menu from an earlier session, correctly gated on Edit
Mode being on. I verified it live regardless (opened a real card's menu as
admin, tapped Edit, confirmed the right product's real data loaded) since
Batch 4 asked for it explicitly and I wasn't going to take "looks done in
the code" on faith after Part 1's history this batch.

PART 7 — Combine existing products into one product with variants

Built as a genuinely new admin flow, additive to (never replacing) the
existing manual "add a variant by hand" editor — both work side by side on
the same product, verified.

Flow: a "Select" button in the admin product list turns on checkboxes; with
2+ selected, a "Combine into variants" bar appears; it opens a confirmation
screen showing every selected product's photo/name/price side by side, lets
you pick which one's Description/How to Use/Key Ingredients/Video/Category/
Skin Type/Skin Condition become the combined product's shared content
(defaults to the first one picked, changeable), and asks for a Region and/or
Size label per product plus the combined product's own name.

On confirm: a brand-new product row is created — its shared content, and
its own price/stock/image/note/Region/Size, come directly from whichever
product you picked as the content source (so that one becomes the first
selectable option automatically, the same mechanism as Part 5's state 3,
not a special case). A real variant row is created for every OTHER selected
product, carrying that product's own price/stock/image/note. Every
originally-selected product (including the content source) is switched to
Active = OFF — never deleted, exactly as specified. Nothing in the flow can
delete a product row.

I chose this "content source's own data becomes the base, not a duplicate
variant row" design deliberately: the alternative (turning every single
selected product into a variant row, including the content source) would
have left the new combined product with an extra, unlabelled "phantom"
option nobody actually picked, on top of the real ones. This way the number
of selectable options always matches exactly the number of products you
combined.

Verified fully end to end on two real catalogue products ("Cancer Council
SPF 50+ Active 200ml Pump" and "Cancer Council 150mL Daily Sunscreen") —
combined them, confirmed the new product's editor opened automatically
showing the merged Region/Size and one real variant row, confirmed both
originals now show Inactive in the admin list and don't appear in a public
search, confirmed the new combined product's detail page shows a working
two-option selector with correct prices (including correctly carrying over
the "Out of Stock" status from the product that was genuinely out of
stock), then reversed my own test afterward — deleted the test combined
product and reactivated both originals, so your real catalogue is back
exactly where it started. (I did hit one real bug while building this —
see section 4.)

PART 8 — Hamburger menu's "Admin Panel" link did nothing

Reproduced this exactly, live: tapping it visibly flashed to /admin for an
instant and then bounced straight back to the homepage.

Root cause: the SAME history-entry mechanism as Part 4's bug, but a
different trigger. The hamburger menu's "Admin Panel" link does two things
in the same click handler: close the menu sheet, then navigate to /admin.
Closing the sheet queues a state update (so its own cleanup — the one that
pops its fake history entry — runs slightly LATER, after React re-renders);
but navigate() pushes the real "/admin" history entry immediately,
synchronously. By the time the sheet's cleanup actually runs and calls
history.back() to remove its own now-stale entry, that entry is no longer
on top of the stack — the /admin navigation is — so the "cleanup" undoes
the navigation instead of its own entry, bouncing you straight back to
where you started.

Fixed in the same shared hook as Part 4, with one more safeguard: each
pushed entry now carries a unique id, and a sheet's cleanup only pops
history if its own entry is still the current one — if something else (like
a real navigation) has since been pushed on top of it, it correctly leaves
history alone instead of undoing the wrong thing.

Verified live, twice: tapped "Admin Panel" from the hamburger menu — it now
lands on /admin and stays there (confirmed the URL, and that the admin
product list actually renders). Also re-checked the ORIGINAL, everyday case
this history mechanism exists for — open a sheet, close it via the X
button, without navigating anywhere — and confirmed it still behaves
exactly as before (the fake history entry is still cleanly removed, no
extra back-press needed later). Fixing the bug did not regress the feature
it sits inside.

PART 9 — Image drag-reorder on both mobile and desktop

Desktop (mouse): dispatched a real mousedown, held 600ms (past the handle's
long-press threshold), moved in several steps, then released — at the
actual coordinates of a real product's image thumbnails. Result: correct
reorder, every time, across two separate tests.

Mobile (touch): this environment's browser automation cannot generate real
hardware touch events, so I dispatched genuine TouchEvent objects (touchstart,
touchmove, touchend, including the required 500ms-plus hold before the
first touchmove) directly at the handle element — the same event types and
sequence a real phone produces, just synthesized rather than from actual
hardware. Result: correct reorder, verified with a clean before/after
comparison (moved image 1 to position 4, got exactly that result, nothing
else changed). I want to be honest about the confidence level here: this
proves the app's own touch event listeners are correctly wired and respond
right — it does NOT rule out something specific to a real touchscreen
digitizer or a particular phone browser's touch quirks, which no
automation can fully substitute for. Given that, and given the earlier
session's report that this was broken, I'd treat this as strong evidence it
now works rather than 100% certainty — a real phone check, if it's easy for
you to do, would close the last bit of doubt.

Persistence: dragged a real product's 5 images into a new order, saved, and
reloaded the actual page from a clean state (not the same tab's cached
view) — the new order was still there. Then reversed my own test drag and
saved again so the product's images are back in their original order.

Before/after, precisely, since this was reported broken without a clear
comparison before: BEFORE this session, drag-reorder had never been
confirmed working on either input type in a real test — only "reported
broken on mobile, never checked on desktop." AFTER this session: confirmed
working on both, with the caveat above about synthetic vs. hardware touch
events. I did not find or need to fix anything in the actual drag-reorder
code itself — it already handles both mouse and touch listeners correctly;
what changed for THIS batch was doing the test properly and reversing it
cleanly.

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
None needed — migration-016-variant-details-and-labels.sql was written this
session and you already ran it successfully partway through (I confirmed
via a live save that the new columns exist and work). Nothing further to
run.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------
- Part 9's mobile touch check is real but synthetic (dispatched TouchEvent
  objects, not a real phone's digitizer) — see Part 9 above for exactly
  what that does and doesn't prove. Everything else in this batch was
  checked in the actual running app with real clicks/taps/saves.
- The cart-button collision check at 320px/360px/390px (Part 2) used the
  real rendered card CSS resized to those exact pixel widths, not an actual
  narrow phone viewport — this Chrome window won't shrink its own viewport
  below about 400px CSS pixels no matter how small I ask it to be (a
  browser minimum, not something in the app). I'm confident in the result
  (it's the real CSS at the real width, just not inside a real 320px-wide
  window frame) but wanted you to know the exact method.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- Prices on the product card are still orange (#FF7A45), not near-black —
  this directly contradicts CLAUDE.md's own colour rule ("Prices are
  near-black and bold, not orange... roughly one orange element per
  screen"). It predates this session and nothing in this batch asked me to
  fix it, so I left it alone, but it's worth knowing it's still there —
  right now a card has at minimum 3 orange elements (price, save badge,
  cart button) before you even count anything else on screen.
- The "combine into variants" flow's bottom action bar, on my first build,
  rendered UNDER the admin's own fixed bottom navigation on mobile (same
  z-index class of bug as the history stuff — two UI layers both wanting
  the bottom of the screen) — a tap meant for "Combine into variants"
  actually landed on the nav bar underneath it. Caught this because I
  tested it live and the app jumped to the Reviews tab instead of opening
  the combine screen. Fixed by giving the bar enough bottom clearance to
  sit above the nav on mobile, and only that much on desktop (where the
  nav becomes a left sidebar and there's nothing to clear).
- My first version of the "combine" flow's final step (re-fetching the new
  product so its editor could open automatically) queried the products
  table directly with select('*') — which silently fails, because the
  wholesale_price column has zero read access at the table level (by
  design, from a much earlier session's security work) and '*' tries to
  read it anyway. Caught this because the flow's LAST step (auto-opening
  the new product) just quietly didn't happen on my first real test, even
  though the product itself was created correctly. Fixed by using
  products_view (the same sanctioned read path every other screen in the
  app already uses) instead of the base table.
- The variant edit form's automated-testing tool (not the app itself)
  occasionally returned stale references to form fields from a previous
  dialog instance after closing and reopening a sheet — cost me some extra
  verification round-trips but is a quirk of the browser-automation tooling
  I used to test with, not anything in your app's code.

--------------------------------------------------------------------
5. DESIGN DECISIONS
--------------------------------------------------------------------
- Part 1's line-splitting is done in JavaScript (measuring real text width
  with a canvas), not CSS. I want to be upfront that this is a bigger
  hammer than a CSS tweak — the reason is in Part 1 above: CSS genuinely
  cannot express "prefer whole words, but break one specific word if
  skipping it whole would waste the rest of the line," which is exactly
  what a two-line card with truncation needs. I checked this claim by
  testing the actual behavioural difference in the real browser before
  writing any code, not just reasoning about it.
- Part 4C's ?variant= URL parameter uses the variant's real database id (a
  UUID) rather than a human-readable slug like the task's own example
  (?variant=usa-473ml). A slug needs its own generation/uniqueness rules
  (what happens with two variants that would produce the same slug, special
  characters in a region name, etc.) and the id already does the actual
  job asked — a link that reopens to the right variant — with zero new
  edge cases. Happy to build a prettier slug later if you would like the
  URL to read nicer when shared.
- Part 5's variant selector on the detail page changed from a two-tier
  Region-then-Size grid to a single flat row of chips (each chip shows its
  full "Region - Size" label). This was a necessary side-effect of adding
  Part 7's combine flow: a combined product's options don't necessarily
  share a clean region-by-size grid the old two-tier picker assumed (e.g.
  one option might have no region at all, or the options might be
  genuinely unrelated sizes from different original products) — a flat
  list handles every case correctly, including the common, clean case,
  without the old layout's assumption. Existing products with real
  region/size variants will look slightly different (one row of chips
  instead of two) but work the same way.
- Part 7's combine flow deliberately does NOT touch the manual "add a
  variant by hand" editor at all — they're two separate, independent ways
  to end up with variants on the same product, exactly as the task asked,
  and I verified both still work side by side on the same product.

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------
1. [Design consistency — small, carried over] Prices are still orange, not
   near-black, on the product card (see section 4) — this is CLAUDE.md's
   own documented rule being violated by existing code, not new work.
   Fixing it is a small, contained CSS change whenever you want it done.
2. [Trust/conversion — medium] Now that a product can genuinely be "two
   size options of the same item" (Part 5/7), it might be worth a future
   session double-checking the WhatsApp order text and PDF export actually
   say which variant was chosen clearly enough for you to fulfil the order
   correctly without opening the app — I confirmed the data is there
   (variantLabel is already threaded through), just flagging it is worth a
   real look at the exact wording once real combined products exist.
3. [Data entry — small] The combine flow's per-product Region/Size fields
   only pre-fill from a product's EXISTING region/size (if it already had
   one set) — there is no attempt to guess a label from the product name
   itself (e.g. spotting "200ml" in "Cancer Council SPF 50+ Active 200ml
   Pump" and suggesting it as the Size). I judged a real name-parsing
   heuristic too risky to build well in the time this batch had — it would
   need to handle a lot of real-world messiness in your product names to
   avoid suggesting something wrong — but it is a genuine time-saver if a
   future session wants to build it carefully.
4. [Code health — small] Two different admin screens (ProductForm.tsx, the
   original /admin product list editor, and ProductEditSheet.tsx, the
   newer inline one opened from a card's "..." menu) both now duplicate the
   same Region/Size fields and both embed VariantEditor separately. This
   was already true before this session for most of the other fields
   (retail price, stock, etc.) — not something this batch introduced — but
   worth knowing the duplication exists if a future session ever wants to
   consolidate down to one product editor.

====================================================================


====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-5-regressions-and-drawer-menu
====================================================================

This session worked through "Batch 5 — Re-fixes, Regressions, and Hamburger
Menu Redesign" (8 parts). Per the batch's own mandatory verification rule, I
did not write "done" for anything without actually loading the real running
app, logging in as admin, performing the real action, and looking at the
real rendered/queried result. At the end I went back through every part a
second time, after everything else was built, specifically checking whether
a later part's change had broken an earlier one — this is documented in
each part below, and it genuinely caught something (see Part 5).

One important thing to flag up front: when I started this session, my own
local copy of the repo was a commit behind — batch 3 and batch 4 had NOT
yet been merged into main in my local clone. I fetched fresh from GitHub and
confirmed both were in fact merged (PR #12 and #13), so this branch starts
from the real, current main. I mention this only so you know I checked it,
not because it turned out to be the explanation for anything below — the
problems in this batch were real bugs, not stale code.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Card name still wasting line-1 space (third attempt)

First confirmed the fix from last session IS wired into the live component
— src/lib/textWrap.ts is imported and called from ProductCard.tsx, not
dead code sitting unused. So the question was why it was still producing a
bad result.

I measured the exact product you'd know as "Aveeno Baby Daily Moisture
Fragrance Free Lotion 227g" in the real running grid at a sweep of widths
(320 through 428px, the realistic phone range) rather than just the one
width (390px) I'd checked last time. At 390px it was already fine — which
is exactly why last session's check missed this. At 360px specifically, it
broke: "Aveeno Baby Dai" / "ly Moisture Fra…" — a real mid-word split,
"Daily" torn into "Dai" + "ly".

The actual cause, with real numbers: at 360px the available line-1 width is
94.4px. "Aveeno Baby " (with trailing space) uses 75.7px, leaving 18.7px of
room. "Dai" (3 characters of "Daily") fits in exactly that 18.7px; the
remaining "ly" is 2 characters. My rule from last session only required
each side of a mid-word break to be at least 2 characters — which "Dai" (3)
and "ly" (2) technically satisfy, so the algorithm judged it an acceptable
break. It isn't, in practice — splitting a plain 5-letter word for the sake
of 2 extra characters of line-1 fill still reads as broken. I raised the
minimum from 2 to 3 characters on each side of a break (src/lib/textWrap.ts,
MIN_FRAGMENT_CHARS). At 3, this exact case now correctly falls back to
moving "Daily" whole to line 2 instead of splitting it, at every width from
320 to 428px.

Verified: swept 320/330/340/350/360/370/375/380/390/400/414/428px again
after the fix — "Aveeno Baby Daily Moisture Fragrance Free Lotion 227g" now
reads as clean whole-word wraps at every one of them (e.g. at 360px:
"Aveeno Baby" / "Daily Moisture…"; at 390px: "Aveeno Baby Daily" /
"Moisture Fragran…"). Also re-ran the full-catalogue orphan/short-fragment
check (104 real cards) at 320/340/360/375/390px — the only two flagged
results ("The Ordinary O. Reusable Rounds…" showing "O." as a line-2 start,
and "Kirkland Minoxidil 5%…" showing "5%") are real whole words in those
products' actual names, not broken fragments — confirmed by checking the
real product names, not assumed. Re-checked again at the very end of this
session, after every other part was done, and it's still clean.

PART 2 — Deleting a combined product now reactivates its originals

Confirmed the gap first: the "combine into variants" flow (built last
session) sets originals inactive but never recorded which product a
combined product or its variants actually came from — so there was no way
for a later delete to know what to reactivate.

Added two new nullable columns (migration-017, SQL below, already run this
session): products.combined_from_product_id (set on a product CREATED by
the combine flow, pointing at whichever original supplied its shared
content and base price — the one that becomes option zero, not a separate
variant row) and product_variants.source_product_id (set on a variant row
created by the combine flow, pointing at the original it was copied from).
Both are null for anything created the ordinary way — a manually-added
variant never sets source_product_id, so deleting a product that only has
hand-added variants is completely unaffected; I did not touch that path.

Deleting a product (src/components/admin/ProductList.tsx) now reads its own
combined_from_product_id plus every one of its variants' source_product_id
BEFORE the delete (variant rows are cascade-deleted the instant the product
row goes), collects the distinct original product ids, deletes as before,
then sets is_active back to true on every one of them.

Verified live, fully: combined "Cancer Council 150mL Daily Sunscreen…" and
"Cancer Council SPF 50+ Active 200ml Pump" into one product (both went
Inactive, confirmed in the real admin list). Deleted the new combined
product. Saw the toast read "Product deleted — 2 original products
reactivated", then independently re-checked the admin list itself (not just
the toast) — both original products showed Active again, with no manual
toggling on my part. Product count back to the original 106.

PART 3 — Hamburger menu: bottom sheet → left-side sliding drawer

Built a new shared component, src/components/shared/SideDrawer.tsx —
same contract as the existing BottomSheet (isOpen/onClose/title/children,
focus trap, Escape, scroll lock, useModalBackClose for the back button) but
slides in from the left over a dimmed backdrop instead of up from the
bottom. BottomSheet itself is untouched — the review form and the bottom-
nav account sheet still use it exactly as before; only HamburgerMenu.tsx
was switched over. Every existing menu item (Dark Mode, Settings, About,
Admin Panel, Wholesale Account) moved across unchanged — same labels, same
onClick handlers, same admin/wholesaler gating.

Animation: 280ms, transform + opacity only (translateX), the close
animation is the exact reverse of the open one, and prefers-reduced-motion
gets an instant show/hide instead (verified the CSS handles this the same
way the existing bottom sheet does, including forcing full transparency on
the closing state so a reduced-motion user doesn't see it freeze in place
for the unmount delay).

Verified live at real phone width (390px): drawer width measured at 328px
= 82% of the 400px viewport, inside the 75–85% requirement, with a visibly
dimmed strip of the page on the right. Tapped a real link inside it (Admin
Panel) — navigated correctly and the drawer closed. Re-opened it and used
the browser back button — drawer closed, no navigation happened, confirmed
via the actual URL staying on "/". Tapping the dimmed backdrop area also
closes it — checked separately. I also made the hamburger button itself a
real open/close toggle (aria-expanded, aria-label switches between "Open
menu"/"Close menu") for correctness, but I want to be upfront that once the
drawer is open it visually covers that same corner of the screen, so
"tapping the hamburger icon again" isn't something a real tap can reach in
this layout — the X inside the drawer, the dimmed backdrop, and the back
button all do the same job and all three are confirmed working. I judged
forcing the header above the drawer's dimming (the only way to make the
original icon reachable) would look worse — an undimmed strip of header
sitting on top of an otherwise-dimmed page — than just relying on the three
ways that do work.

PART 4 — Admin Panel navigation: no more jump/bounce

Root cause, confirmed by reading the code rather than guessing: AdminPage
never reset scroll position on mount — every other page that can be reached
by a fresh navigation (e.g. ProductDetailPage) does this already, AdminPage
was simply missing the same one-line effect. If you'd scrolled down the
home page before opening the menu, landing on /admin kept that same scroll
position, which is what read as a jump.

Added the same window.scrollTo(0, 0) effect AdminPage was missing (runs
once on mount, not on every tab switch inside the admin panel).

Verified live: scrolled the home page down a long way, opened the new
drawer, tapped Admin Panel — landed on /admin with window.scrollY at
exactly 0 and the real admin header visible at the very top, no visible
jump. Re-checked again at the end of the session after every other part was
built, from a fresh page load — still lands at the top cleanly.

PART 5 — Variant image now actually shows on the detail page

Reproduced it exactly: uploaded a real photo to a real variant (via the
same admin flow you'd use), selected that variant on the real product page
— the hero image did NOT change, kept showing the product's default image.

Root cause, found by inspecting the live DOM rather than assuming: the
image swap logic itself was correct — the variant's photo really was being
placed first in the array. What was actually happening is the browser's
own "scroll anchoring" feature: when the slide order changes, the browser
tries to be helpful by keeping whatever was already visible on screen in
view, and silently scrolls the carousel forward by exactly one slide-width
to compensate — which cancels out the swap, so you keep seeing the old
image even though the underlying data changed correctly. I confirmed this
by reading the carousel's actual scrollLeft value right after switching
variants: it had moved to 388px on its own, nobody told it to.

Fixed with two things in src/pages/ProductDetailPage.tsx: the carousel now
resets its own scroll position back to the start every time the selected
variant actually changes (not on every render), and I added
overflow-anchor: none in the CSS so the browser stops trying to compensate
in the first place.

Verified live: selected a variant with its own uploaded photo — the hero
image genuinely changed to that photo (confirmed by reading the actual
image src, and by looking at the rendered screenshot — the recognisable
"Naeem's — Authentic Australian skincare" branded image I'd uploaded as the
test photo was really on screen, not the product's usual bottle photo).
Switched back to the option with no photo of its own — correctly fell back
to the product's normal image. Re-verified a second time at the very end of
the session, after Part 6 rewrote the selector component in the same file —
this is the one place in this batch where the "check it again at the end"
rule actually caught something worth catching: Part 6 touches the exact
same file Part 5's fix lives in, so I re-tested Part 5 specifically after
Part 6 was done, not just assumed it still worked. It does.

PART 6 — Variant selector reverted to two separate levels (Region, then Size)

Replaced the flat single row of chips with the original two-level layout: a
Region row, then a Size row filtered to whatever that Region actually has —
but rebuilt to work off last batch's merged option list (a product's own
data plus its real variant rows) rather than raw variant rows only, and
each row now only appears when there's more than one distinct value to
choose between (so a product that only varies by Region shows just the
Region row, and vice versa — this wasn't gated at all in the original
pre-flat version, so it's slightly improved from before, not just restored).

For the case the two-level grid genuinely can't represent — an option with
a blank Region or blank Size, which happens when a product has no label set
at all (it falls back to showing its bare name) or when the "combine
products" flow was only given one half of a label — the selector correctly
drops back to the flat, one-row-per-option list rather than showing a
broken or misleading grid. This isn't a guess: I tested both shapes on the
same real product.

Verified live, in detail, on "Beauty of Joseon Red Bean Water Gel" with two
real variants set up as AU/350mL (the product's own base data), AU/473mL,
and US/473mL: saw two separate rows, "Region" (AU, US) and "Size" (350mL,
473mL), default selection AU/350mL. Clicked "US" — the Size row correctly
disappeared (US only has one size) and price/URL updated to the US variant.
Clicked back to "AU" — Size row reappeared showing both sizes, and it kept
"473mL" selected rather than resetting to the region's first option, because
I built pickRegion to prefer keeping the same size across a region switch
when that size exists in the new region too; price and the URL's ?variant=
both matched the correct variant at every step. Separately confirmed the
flat fallback still works on the same product before I'd given it a clean
label (two chips, "Beauty of Joseon Red Bean Water Gel" and "US · 473mL",
full names, no Region/Size rows) — so both code paths were actually
exercised, not just the common one.

PART 7 — Image drag-reorder: found a real gap, not a re-run of the same bug

This is the one where reading the code first actually mattered. The
drag-reorder files themselves (useDragReorder.ts, ImageStrip.tsx) hadn't
been touched by any commit since long before last session — so whatever
last session tested and reported working was, in that exact code, still
true. But there are TWO separate product editors in this app: the inline
one opened from a card's "..." menu on the home grid (ProductEditSheet.tsx,
uses ImageStrip — has drag-reorder) and the one opened from the admin
product list's own "Edit" button (ProductForm.tsx, uses a completely
different component called ImageUploader — which never had drag-reorder at
all, just add/remove). Last session's testing (and my own, until I noticed
this) only ever went through the first path. The admin product list's own
Edit button — almost certainly the way you'd naturally reach it from
/admin — genuinely had no way to reorder images. That's the real "it
stopped working" here: it never worked from that entry point.

Fixed by wiring the same drag mechanism into ImageUploader.tsx. One
necessary side effect: ImageUploader's image list used to wrap into a
multi-row grid, and the drag-reorder mechanism (shared with ImageStrip)
only understands one-directional dragging, not a 2D wrapping grid — so I
changed it to a single horizontal scrolling row, the same shape
ImageStrip already uses. Same thumbnail size, same remove button, same
"Add" button at the end — just scrolls sideways instead of wrapping when
there are more than fit on one row.

Verified live on both entry points, mouse and touch, with a full save-and-
reload persistence check each time — and I want to be specific about a
testing wrinkle I hit and resolved, not just the end result: my FIRST
attempt at testing the admin-list path (right after editing the code)
showed drag doing nothing at all. Before concluding that meant the fix was
broken, I did a full page reload and tried again — it worked immediately
and correctly, every time afterward. That first failure was stale hot-
reload state from editing the component live, not the actual fix; I'm
noting it explicitly because this batch exists precisely because past
sessions didn't chase down exactly this kind of "looks broken, might not
be" moment. After the reload, I dragged a real image to a new position,
saved, reloaded the page fresh (not the same tab's cached view — a full
navigation to the product's public detail page), and the new order was
still there. I then manually restored the original image order (three
separate drag-save-reload cycles, each independently verified) so the
product's real images are exactly as they were before I touched them.

PART 8 — "Edit" in the home grid card's "..." menu

Checked the code first: CardMenu.tsx already has an "Edit Product" entry,
gated on Edit Mode being on (the same real admin/session check every other
admin-only control in this app uses — not CSS, not guesswork). I used this
exact menu item successfully, repeatedly, throughout this session to reach
the product editor for Parts 5, 6 and 7's testing above, so by the time I
sat down to check this part specifically it had already been proven working
many times over. I still did one clean, explicit check for the record:
opened Edit Mode, opened a real card's "..." menu, confirmed "Edit Product"
is the first item, tapped it, and confirmed the product editor opened
showing that exact product's real name and data.

I don't have a code-level explanation for why this would have looked
missing on the live site, the way I found concrete causes for the other
parts — the option is real, present, and working in the current code with
no gap I could find. I'm reporting that honestly rather than inventing a
fix for a bug I couldn't reproduce.

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
None needed — migration-017-combine-source-tracking.sql was written this
session and you already ran it successfully partway through (confirmed via
the live combine-then-delete test in Part 2 that the new columns exist and
the reactivation actually works). Nothing further to run.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------
- Part 3's "tap the hamburger icon again to close" — see Part 3 above for
  exactly why (the icon's own screen position is covered by the open
  drawer) and what I verified instead (X button, backdrop tap, back button
  — all three confirmed working). I did make the underlying button a real
  toggle in the code, but by the layout's own nature that toggle isn't
  reachable through a second tap once the drawer is open.
- Part 8 — I could not find anything actually wrong to fix. Reported
  honestly above rather than making an unnecessary change to working code.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- The real, recurring pattern behind this batch, now that I've dug into
  four separate "was fixed, now isn't" reports: two were genuine bugs that
  only showed up outside the exact conditions previously tested (Part 1's
  360px-only break; Part 7's admin-list entry point never having had the
  feature at all) — not regressions in the sense of working code being
  undone, but real gaps that narrower testing missed the first time. A
  third (Part 5) was a subtle browser behaviour (scroll anchoring) that
  looks identical to "the code didn't work" from the outside but has
  nothing to do with the actual swap logic. The fourth (Part 8) I could not
  reproduce at all. I don't have hard evidence for this, but it's worth
  knowing: this app is a PWA with a service worker that caches the built
  app (registerType: 'autoUpdate', which is already the safer of the two
  common settings — it self-updates rather than waiting for a prompt). If
  you're testing on a phone or a browser tab that's been open a while
  rather than a completely fresh load, it's possible to be looking at a
  cached previous build for a short window after a new one ships. I can't
  confirm this was the explanation for anything specific in this batch, but
  if a future "definitely fixed this already" report comes up again, a
  hard refresh (or closing and reopening the tab/PWA) before concluding the
  fix failed would rule this out cheaply.
- Confirmed a duplication I flagged last session is real and now matters
  more: ProductForm.tsx and ProductEditSheet.tsx are genuinely two separate
  product editors with two separate image-handling components
  (ImageUploader vs ImageStrip). Part 7 fixed the immediate symptom (drag-
  reorder now works in both), but they're still two components doing
  almost the same job with separate code paths — any future change to one
  (like Part 5/6's variant work, which only touches the product DETAIL
  page, not this) needs to be remembered for the other too.

--------------------------------------------------------------------
5. DESIGN DECISIONS
--------------------------------------------------------------------
- Part 2: the combined product's own base data (price/stock/image/note)
  still comes directly from whichever original was picked as the "shared
  content source," rather than every selected product becoming a real
  variant row — this is unchanged from last session's design and Part 2
  builds on it directly (combined_from_product_id records that one
  relationship, source_product_id records the rest).
- Part 3: 82% width, capped at 340px on wider screens (so the drawer
  doesn't get unreasonably wide on a tablet or desktop browser window) —
  the requirement was 75–85% and this is a straightforward middle choice
  within it.
- Part 7: turning ImageUploader's grid into a horizontal scrolling row
  (matching ImageStrip) was the one place in this batch I made a layout
  change beyond the minimum needed to fix the reported bug. I want to be
  upfront about why: the reorder mechanism this app already has and trusts
  only understands dragging along one axis, and a wrapping multi-row grid
  needs real two-dimensional hit-testing to reorder correctly by drag —
  building that from scratch risked a worse, buggier drag experience than
  none at all. Matching the pattern that's already proven to work
  correctly elsewhere in this exact app was the safer choice over inventing
  new drag logic under this batch's time budget.

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------
1. [Code health — medium, escalated from last session] Two separate
   product editors with two separate image components is no longer just a
   tidiness question — Part 7 this session was a direct, real consequence
   of it (a feature working in one and silently not existing in the other).
   The same risk applies to anything else added to one editor without the
   other in mind. Worth a session dedicated to merging them into one editor
   used everywhere, which would remove this whole class of bug permanently
   rather than one symptom at a time.
2. [Process — worth repeating] The pattern behind this entire batch was
   narrower testing conditions than real usage — one screen width, one of
   two entry points, one browser tab that happened to already have the new
   code. None of that is a criticism of testing "live" in principle (it
   caught real, specific bugs this session, with exact pixel numbers each
   time) — it's that "verified live" needs to mean a spread of realistic
   conditions (a few widths, both admin entry points where more than one
   exists, a genuinely fresh page load), not just one successful pass.
3. [Trust signal — small] Still true from two sessions ago: product card
   prices are orange, not near-black, against CLAUDE.md's own colour rule.
   Not touched this batch (out of scope), just flagging it's still
   outstanding.

====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-6-card-name-and-edit-menu
====================================================================

This session worked through "Batch 6 — Card Name Gap and Missing Edit
Option (Verify on Real Production URL)" (2 parts). You told me both items
had been reported fixed twice before and were still broken for you on
https://naeems-all.vercel.app in a brand-new browser, so the instruction
was explicit: don't trust the old fixes, re-diagnose from scratch, and
don't write "done" without a concrete description of what the real
production site actually showed. Everything below was checked against
that exact URL, using real DOM measurements and real canvas text
measurements taken from the live page — not assumptions from reading the
source code.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Card name still leaving unused space on line 1

First I checked whether the two earlier "fixes" had even made it to
production, because that's the single most common reason a fix looks
undone. My local clone's cached copy of the GitHub main branch was a
commit behind (it still showed Batch 5's branch as unmerged) — but a
fresh `git fetch` showed PR #14 (Batch 5) genuinely was merged, and
Batch 5's code (the left-side drawer menu, the MIN_FRAGMENT_CHARS
change) is confirmed live and running on production right now. So this
was not a missed-deployment problem. I mention the false alarm because
it's exactly the kind of thing that caused the last two sessions'
confusion, and I want you to know I checked it properly with a fresh
fetch rather than trusting a stale local branch list.

With that ruled out, I tested the font-loading race theory the task
asked me to check first, directly on production: I read
`document.fonts.status` on the live page after load — it reported
"loaded" — and then re-ran the exact same canvas measurement the site's
own code uses, with the exact same font string, and got the exact same
(wrong) line break the page was already showing. That means the font
was NOT still loading when the bad split happened. The font-race theory
was a reasonable thing to check but it is not what's happening here.

The real cause, found by measuring "CeraVe Foaming Cleanser
Eco-Recharge Refill 473mL" on the real live grid: the card's usable
line-1 width left 40.7px of room after "CeraVe Foaming". The next word,
"Cleanser", could fit 6 characters ("Cleans") in that space, which
would leave "er" (2 characters) for the start of line 2. Batch 5's rule
requires at least 3 characters on both sides of a mid-word break, so
"Cleans"/"er" correctly got rejected — but the code's response to that
rejection was to give up on splitting the word at all, leaving "CeraVe
Foaming" alone on line 1 with 40px of empty space, instead of trying a
slightly shorter, still-valid split. A 5-character piece ("Clean") would
have left "ser" (3 characters) — which passes the same rule Batch 5
already had — but the code never tried it, because it only ever computed
the single longest-fitting prefix and gave up entirely if that one
specific prefix failed the check.

The fix (src/lib/textWrap.ts): instead of only trying the longest
prefix that fits the available pixels, it now caps that prefix at
`word.length - MIN_FRAGMENT_CHARS` before checking it — which finds the
largest prefix that BOTH fits the available width AND leaves the
required 3 characters on line 2, in one step, rather than trying only
the biggest option and giving up if that one fails.

I separately added a defensive fix in src/components/viewer/ProductCard.tsx:
even though the font race wasn't the cause of this specific bug, nothing
in the code currently re-measures if the card's font swaps in after the
first paint (a real risk with the Google Font this site uses, which
loads asynchronously). I added a `document.fonts.ready.then(measure)`
call so a late font swap now triggers a correct re-measurement instead
of leaving whatever was measured against the fallback font. This didn't
fix today's reported bug, but it closes a real gap the task specifically
asked me to check for.

How I verified it, precisely: I could not watch my own new code run at
the literal https://naeems-all.vercel.app URL in this same session,
because that only updates once you merge this branch's pull request
into main (see the honesty note in section 6 — I am not allowed to merge
to main myself, and structurally no session can). What I did instead:
I pulled the real rendered widths, the real computed font string, and
the real product names straight from the live production page right
now, then ran both the OLD and the NEW splitting logic against that
real data side by side, in the browser, to see exactly what each one
produces:
  - "CeraVe Foaming Cleanser Eco-Recharge Refill 473mL" at the real
    134.7px card width: OLD gave "CeraVe Foaming" / "Cleanser
    Eco-Rechar…" (the reported bug, empty space on line 1). NEW gives
    "CeraVe Foaming Clean" / "ser Eco-Recharge Refi…" — line 1 now fills
    the available width with a valid break.
  - "Cerave PM Face Moisturizing Lotion 89ml" at the real 141.5px width:
    unchanged by the fix, both give "Cerave PM Face" / "Moisturizing
    Lotion 89ml" — this one was already fine at this width, confirming
    no regression.
  - "Aveeno Baby Daily Moisture Fragrance Free Lotion 227g" and the
    "…Lightly Scented Wash" variant, both at the real 141.5px width:
    also unchanged by the fix (both already split cleanly as "Aveeno
    Baby Daily Moist" / "ure …" with under 5px of unused space) —
    another confirmed non-regression. I could not reproduce a visible
    gap for these two specific products at the widths this browser
    could render; it's possible Naeem's real phone hits a narrower
    width where the same underlying flaw showed up differently, but the
    class of bug (give up on a valid shorter split) is now fixed
    generally, not tuned to one screenshot.
`npm run build` (which runs the TypeScript compiler, then Vite) passed
with zero errors after the change.

PART 2 — "Edit Product" missing from the home grid card menu

I checked git history first: the code for this menu item
(src/components/viewer/CardMenu.tsx) was added in a commit from before
Batch 1 and has not been touched or removed by any commit since,
including Batch 4 and Batch 5. So this was never a regression — the
code has been sitting there, unchanged and correct, the whole time.

The Chrome browser I used for this session already had an active admin
session on it (I saw the admin-only "Edit Mode" floating pencil button
appear on the homepage before I logged in as anyone), so I was able to
test your exact real situation end to end without needing your password
for this part. Here's exactly what I found: "Edit Product" is gated
behind a site-wide "Edit Mode" toggle (src/contexts/AdminEditContext.tsx),
switched on and off by a floating pencil-shaped button fixed above the
bottom navigation bar, visible only to an approved admin. It is off by
default and resets to off every time you reload the page or sign out —
it does not stay on. The same toggle also controls every other inline
edit control across the site (the hero banner, the browse category
circles, the bento grid tiles, the expert contact page) — it's one
consistent switch for all inline editing, not something specific to
just this menu.

With Edit Mode off, tapping "..." on a real product card on the real
home grid showed exactly what you described: Share Product, Copy Link,
Copy Price, and a disabled Save — no Edit Product. I then tapped the
pencil button to turn Edit Mode on (a thin orange bar appeared across
the top of the screen, confirming it was on), tapped "..." on the same
CeraVe card again, and "Edit Product" was there. I tapped it, and the
real product editor sheet opened, pre-filled with that exact product's
real data (name, brand, category, retail/offer/wholesale prices, stock
count) — confirmed correct, not a placeholder.

So: this was never broken code. It's a real feature that's always been
there, gated behind a toggle you didn't know existed. I decided not to
remove the toggle requirement, because it's the one consistent
mechanism protecting every inline-editing surface on the site from
accidental edits while you're just browsing normally as admin — removing
it just for this one menu item would make Edit Product behave
differently from every other inline edit control on the site, which
seems like it would create more confusion, not less. The floating pencil
button is already visually obvious once you know to look for it (bottom
right, above the cart/account bar, only shows for you as admin) — the
real gap was nobody had told you it exists. Now you know: **tap the
pencil icon bottom-right to turn Edit Mode on, then "..." on any card
will show Edit Product.**

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
None needed. Nothing in this batch touched the database.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------
I could not watch my own Part 1 code fix running live at the actual
https://naeems-all.vercel.app URL, because that URL only updates after
you merge this branch's pull request into main, and I'm not able to
merge to main myself (by design — see CLAUDE.md's git rules). What I
verified instead is described in detail in Part 1 above: real
production data (real widths, real fonts, real product names) run
through both the old and new code side by side, plus a clean local
build. Please do one thirty-second check yourself after merging: open
https://naeems-all.vercel.app on your phone in a fresh/incognito tab and
look at the CeraVe Foaming Cleanser card — line 1 should now read
"CeraVe Foaming Clean" (or similar, filling the line) instead of
stopping short at "CeraVe Foaming" with empty space.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- The two "still broken" bugs in this batch turned out to have two
  completely different real causes, and neither was what the task
  document's leading theories suggested. Part 1 was a genuine remaining
  logic gap in the split algorithm (not the font race that was
  flagged as the top suspect). Part 2 was not a bug at all — the code
  has been correct and unchanged since before Batch 1; it was a
  feature-discoverability gap. Recorded honestly rather than forcing a
  fit to the suspected causes.
- See section 6 for the direct answer to your question about whether
  there's a structural gap between what a session can verify and what
  actually ends up live for you.

--------------------------------------------------------------------
5. DESIGN DECISIONS
--------------------------------------------------------------------
- Part 1: chose to fix the line-splitting heuristic itself (search for
  the largest valid prefix instead of only trying the single longest
  one) rather than lowering MIN_FRAGMENT_CHARS again or changing the
  card's width/padding. Lowering the minimum would reintroduce the
  "Dai"/"ly"-style orphan breaks Batch 5 fixed; changing card width
  would be a much bigger, unrelated layout change. Fixing the heuristic
  fixes the actual flaw without touching anything Batch 5 already got
  right.
- Part 1: also added the font-ready re-measure even though it wasn't
  the cause of today's specific bug, because it's a real, currently
  unguarded gap the task explicitly asked me to check for, and it's a
  small, low-risk addition (one extra promise callback) rather than a
  redesign.
- Part 2: chose to keep the Edit Mode toggle requirement rather than
  making Edit Product always visible to any admin, because it's a
  deliberate, consistent, site-wide mechanism (five other components
  share it) rather than something specific to this one menu — see the
  full reasoning in Part 2 above. If you'd rather Edit Product always
  show without needing to toggle Edit Mode on first, tell me and I can
  make that one-line change next session.

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------
1. [Process — directly answers your question] You asked whether there's
   a real structural reason two "fixed and verified" reports didn't hold
   up for you in production. The honest answer is a mix of both:
   - For Part 2, there was NO gap at all — the code was correct and
     live the whole time; it just needed a "here's exactly how you'll
     see this" explanation, which apparently never made it into your
     hands clearly enough. That's on how it was communicated, not the
     code or the deploy pipeline.
   - For Part 1, both earlier fixes were real, correctly deployed fixes
     for the specific cases each session tested — but a heuristic
     algorithm like this one has many possible word-length/width
     combinations, and each session's test sweep missed the specific
     combination that was still broken. That's a test-coverage gap, not
     a deployment gap.
   - There IS one genuine structural limit worth knowing, though, and
     it's not specific to this session: no session can ever watch its
     OWN new code running at the real production URL in the same
     session it wrote that code, because CLAUDE.md correctly forbids
     pushing to main directly, and only main deploys to
     naeems-all.vercel.app. A session can build locally, and can test
     its new logic against real data pulled from the live site, but the
     literal "open the real URL and see the new code live" step can
     only happen after you merge the PR. Any future session that claims
     "verified live" for code it JUST wrote in that same session should
     be read with that caveat — the only way to fully close this gap is
     for you to do a quick check yourself right after merging, which is
     exactly what I've asked for in section 3 above.
   Small process suggestion: when you merge one of these batch PRs,
   it'd be worth doing that one thirty-second phone check the same day,
   before the next batch starts — that's the one point where a real bug
   and a "not deployed yet" bug look identical from a chat transcript.
2. [Code health — small] src/lib/textWrap.ts is now handling a
   surprisingly deep set of edge cases (whole-word fit, forced first-word
   break, mid-word spill with a minimum-fragment rule) for a single pure
   function with no automated tests. A handful of unit tests (a handless
   test runner isn't even set up yet — see package.json, there's no test
   script at all) covering the real product names that have caused
   trouble across three sessions now (CeraVe Foaming Cleanser, Aveeno
   Baby Daily Moisture, the "Dai"/"ly" case) would catch a regression
   here in seconds instead of needing another live-production
   investigation next time this file changes. Small effort, meaningful
   payoff given this exact file has now needed fixing in three
   consecutive batches.
3. [Trust signal — small, carried forward] Still true, third session
   running: product card prices are orange (#FF7A45), not near-black,
   against CLAUDE.md's own colour rule (src/styles/app.css line 3722).
   Not touched this batch — still out of scope of what was asked — just
   flagging it keeps not getting picked up.

====================================================================
NAEEM'S PRICE HUB — SESSION LOG
Branch: fix/batch-7-native-text-wrapping
====================================================================

This session worked through "Batch 7 — Delete the JavaScript
Line-Splitter, Use Native CSS Wrapping". The instruction this time was
explicit: stop tuning the JS line-splitter (src/lib/textWrap.ts) that
five straight batches had already adjusted, and remove it entirely in
favour of letting the browser's own line-breaking wrap the card name.
That's what I did.

--------------------------------------------------------------------
1. WHAT I DID
--------------------------------------------------------------------

PART 1 — Removed the JS splitter

- src/components/viewer/ProductCard.tsx: removed the ResizeObserver,
  the document.fonts.ready re-measure callback, the splitProductName/
  fontStringOf imports, and the two-line React state. The product
  name is now a single presentational component:

    export function ProductCardName({ name }: { name: string }) {
      return <h3 className="product-card__name">{name}</h3>;
    }

  I kept it as its own small exported component (instead of inlining
  the <h3> straight into ProductCard) purely so Part 3's test could
  render it without needing to mock Supabase, routing, and the rest
  of ProductCard's context tree — it has no hooks and no dependency
  besides the string it's given.

- Deleted src/lib/textWrap.ts. I grepped the whole repo first —
  nothing else imported it (only ProductCard.tsx and Naeems.txt's own
  history mentioned it), so it was safe to remove outright rather than
  leave dead code behind.

- src/styles/app.css (.product-card__name, around line 3688): replaced
  the old rule (flex column, min-height reservation, two child
  ".product-card__name-line" spans with white-space:nowrap+overflow:
  hidden) with the standard two-line clamp this codebase already uses
  in three other places (.hero-banner__title, .bento-custom__title,
  .bento-tile__name, .review-admin__quote):

    white-space: normal;
    word-break: normal;
    overflow-wrap: normal;
    hyphens: none;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    min-height: calc(2 * 1.35em);   /* unchanged — still reserves 2 lines */

  Deleted the now-unused ".product-card__name-line" rule along with
  its comment block (that comment was the thing explaining why JS
  splitting was supposedly necessary — no longer true).

- Grep for word-break: break-all / word-break: break-word /
  overflow-wrap: anywhere anywhere in the stylesheets, as the task
  asked: the ONLY hit outside the deleted comment was
  ".admin-wholesaler-row__meta { word-break: break-word }" at line
  2404 — that's the admin wholesaler list's join-date/meta text, a
  completely different component, not the product card. Left it
  alone. No break-all or overflow-wrap:anywhere rule targeting the
  card name existed anywhere in the codebase — Batch 5's fix must have
  already been replaced by the JS splitter in a later batch before
  this session started.

- Checked the flex chain around the name element for width-stealing,
  as instructed: .product-card__body is a flex row (padding 9px 10px
  10px, gap 8px) containing .product-card__info (flex:1, already had
  min-width:0 from an earlier batch — no change needed) and
  .product-card__cart-slot (width:34px, flex-shrink:0, its own
  reserved column). This was already correct; I didn't touch it.

- Did not touch font size, letter spacing, card width/padding, or any
  product name in the database, per the task's explicit "do not"
  list.

PART 2 — Verification across real widths

Two things blocked doing this the obvious way (load the real site,
shrink the window):

  1. Only `main` deploys to https://naeems-all.vercel.app, and I'm not
     allowed to push there — this is the same structural limit Batch 6
     already flagged, carried forward, not new.
  2. Less expected: in THIS session's Windows Chrome, the resize_window
     tool could not actually shrink the browser window's viewport below
     about 523px — I tried 400px, 375px, and 320px and window.innerWidth
     read back 523 every time (likely an OS-level minimum window width
     on this machine). So even local, same-session testing at real phone
     widths via literal window resize wasn't available either.

What I did instead: built a throwaway test harness page
(dist/wrap-test.html, deleted again before finishing — it is NOT part
of the commit) that pulls in the actual compiled CSS
(dist/assets/index-*.css) and the actual Google Fonts link, and
renders the exact same DOM structure the real card uses
(.product-card > .product-card__body > .product-card__info >
.product-card__name, plus a same-size .product-card__cart-slot so the
reserved button column is accounted for) inside a wrapper div whose
width I could set to an exact pixel value — 320, 360, 375, 390, 414 —
standing in for the viewport width, since none of those cross the
grid's 480px column-count breakpoint.

I validated this harness against the real thing before trusting it: I
loaded the actual site (localhost:4173, a production build, using
real Supabase data — no mocking) in a normal Chrome window and read
the real DOM measurements directly. At a real rendered card width of
139.67px, the real .product-card__name element measured exactly
76.33px wide. My harness at a simulated 320px viewport (which produces
a 138px card by the same grid math) measured 75px for the name
element — a match. That gave me confidence the harness reflects
production, not a guess.

One correction to the task brief's own numbers, for the record: the
task said "real available width of the name element on his phone:
about 139 CSS px." That 139px figure is actually the CARD's width, not
the name element's — the name column loses 20px to the card's own
left/right padding, 8px to the gap before the cart button, and 34px to
the reserved cart-button column, leaving roughly HALF that: 76px at a
320px screen, up to about 122px at 414px. I don't think this changes
anything about the fix (native CSS wrapping is still strictly better
than the JS splitter at any of these widths — see the table below),
but it's worth knowing the real number if you're ever eyeballing card
width again.

Full measured results, all 8 names × all 5 widths, using getClientRects()
on the real rendered text node (not a screenshot guess) to find exactly
where each line breaks:

  320px (name column ≈75px):
    CeraVe Moisturising Cream 50ml            → "CeraVe" / "Moisturising Cream 50ml…" (overflows 2 lines, ellipsis)
    CeraVe Moisturizing Lotion 236ml          → "CeraVe" / "Moisturizing Lotion 236ml…" (overflows, ellipsis)
    CeraVe Oil Control Moisturising Gel-Cream 52ml → "CeraVe Oil" / "Control Moisturising Gel-Cream…" (overflows, ellipsis)
    Cerave PM Face Moisturizing Lotion 89ml   → "Cerave PM" / "Face Moisturizing Lotion 89ml…" (overflows, ellipsis)
    CeraVe Hydrating Foaming Oil Cleanser 473ml → "CeraVe" / "Hydrating Foaming Oil Cleanser…" (overflows, ellipsis)
    Aveeno Baby Daily Moisture Fragrance Free Lotion 227g → "Aveeno Baby" / "Daily Moisture Fragrance Free…" (overflows, ellipsis)
    Beauty of Joseon Red Bean Water Gel       → "Beauty of" / "Joseon Red Bean Water Gel…" (overflows, ellipsis)
    Bio Oil | 200mL                           → "Bio Oil |" / "200mL" (fits in 2, no ellipsis, full name)

  360px (name column ≈95px):
    CeraVe Moisturising Cream 50ml            → "CeraVe" / "Moisturising Cream 50ml…" (overflows, ellipsis)
    CeraVe Moisturizing Lotion 236ml          → "CeraVe" / "Moisturizing Lotion 236ml…" (overflows, ellipsis)
    CeraVe Oil Control Moisturising Gel-Cream 52ml → "CeraVe Oil" / "Control Moisturising Gel-Cream…" (overflows, ellipsis)
    Cerave PM Face Moisturizing Lotion 89ml   → "Cerave PM Face" / "Moisturizing Lotion 89ml" (fits in 2, no ellipsis, full name)
    CeraVe Hydrating Foaming Oil Cleanser 473ml → "CeraVe" / "Hydrating Foaming Oil Cleanser…" (overflows, ellipsis)
    Aveeno Baby Daily Moisture Fragrance Free Lotion 227g → "Aveeno Baby" / "Daily Moisture Fragrance Free…" (overflows, ellipsis)
    Beauty of Joseon Red Bean Water Gel       → "Beauty of" / "Joseon Red Bean Water Gel…" (overflows, ellipsis)
    Bio Oil | 200mL                           → "Bio Oil | 200mL" (fits on 1 line, full name, no ellipsis)

  375px (name column ≈102px):
    CeraVe Moisturising Cream 50ml            → "CeraVe" / "Moisturising Cream 50ml…" (overflows, ellipsis)
    CeraVe Moisturizing Lotion 236ml          → "CeraVe" / "Moisturizing Lotion 236ml…" (overflows, ellipsis)
    CeraVe Oil Control Moisturising Gel-Cream 52ml → "CeraVe Oil" / "Control Moisturising Gel-Cream…" (overflows, ellipsis)
    Cerave PM Face Moisturizing Lotion 89ml   → "Cerave PM Face" / "Moisturizing Lotion 89ml" (fits in 2, no ellipsis)
    CeraVe Hydrating Foaming Oil Cleanser 473ml → "CeraVe Hydrating" / "Foaming Oil Cleanser 473ml" (fits in 2, no ellipsis)
    Aveeno Baby Daily Moisture Fragrance Free Lotion 227g → "Aveeno Baby" / "Daily Moisture Fragrance Free…" (overflows, ellipsis)
    Beauty of Joseon Red Bean Water Gel       → "Beauty of Joseon" / "Red Bean Water Gel" (fits in 2, no ellipsis)
    Bio Oil | 200mL                           → "Bio Oil | 200mL" (1 line, no ellipsis)

  390px (name column ≈110px):
    CeraVe Moisturising Cream 50ml            → "CeraVe" / "Moisturising Cream 50ml…" (overflows, ellipsis)
    CeraVe Moisturizing Lotion 236ml          → "CeraVe" / "Moisturizing Lotion 236ml…" (overflows, ellipsis)
    CeraVe Oil Control Moisturising Gel-Cream 52ml → "CeraVe Oil Control" / "Moisturising Gel-Cream 52ml" (fits in 2, no ellipsis)
    Cerave PM Face Moisturizing Lotion 89ml   → "Cerave PM Face" / "Moisturizing Lotion 89ml" (fits in 2, no ellipsis)
    CeraVe Hydrating Foaming Oil Cleanser 473ml → "CeraVe Hydrating" / "Foaming Oil Cleanser 473ml" (fits in 2, no ellipsis)
    Aveeno Baby Daily Moisture Fragrance Free Lotion 227g → "Aveeno Baby Daily" / "Moisture Fragrance Free…" (overflows, ellipsis)
    Beauty of Joseon Red Bean Water Gel       → "Beauty of Joseon" / "Red Bean Water Gel" (fits in 2, no ellipsis)
    Bio Oil | 200mL                           → "Bio Oil | 200mL" (1 line, no ellipsis)

  414px (name column ≈122px):
    CeraVe Moisturising Cream 50ml            → "CeraVe Moisturising" / "Cream 50ml" (fits in 2, no ellipsis, FULL name)
    CeraVe Moisturizing Lotion 236ml          → "CeraVe Moisturizing" / "Lotion 236ml" (fits in 2, no ellipsis, FULL name)
    CeraVe Oil Control Moisturising Gel-Cream 52ml → "CeraVe Oil Control" / "Moisturising Gel-Cream…" (overflows, ellipsis — genuinely too long for 2 lines even at the widest tested width)
    Cerave PM Face Moisturizing Lotion 89ml   → "Cerave PM Face" / "Moisturizing Lotion 89ml" (fits in 2, no ellipsis)
    CeraVe Hydrating Foaming Oil Cleanser 473ml → "CeraVe Hydrating" / "Foaming Oil Cleanser 473ml" (fits in 2, no ellipsis)
    Aveeno Baby Daily Moisture Fragrance Free Lotion 227g → "Aveeno Baby Daily" / "Moisture Fragrance Free…" (overflows, ellipsis — this name is genuinely long)
    Beauty of Joseon Red Bean Water Gel       → "Beauty of Joseon Red" / "Bean Water Gel" (fits in 2, no ellipsis, FULL name)
    Bio Oil | 200mL                           → "Bio Oil | 200mL" (1 line, no ellipsis)

Checked against the 5 requirements:
  A. No mid-word break — TRUE across all 40 renders (8 names × 5 widths).
     Every single line boundary lands on whitespace or punctuation, never
     inside a word. Verified programmatically (not by eye): for every
     wrap point I checked that the characters immediately before and
     after the break weren't both non-space characters.
  B. A name that fits in 2 lines shows in full, no ellipsis — TRUE. E.g.
     "Bio Oil | 200mL" never gets an ellipsis at any width; "CeraVe
     Moisturising Cream 50ml" shows completely at 414px with no ellipsis.
  C. Ellipsis appears only where the name genuinely needs a 3rd+ line —
     TRUE. E.g. "CeraVe Oil Control Moisturising Gel-Cream 52ml" still
     needs an ellipsis even at the widest width tested (414px) — that's
     correct, it's a genuinely long name, not a bug.
  D. Card height / row alignment unchanged — TRUE. Checked all 8 name
     elements' rendered heights at 375px: all 8 were exactly 31px,
     including the short one-line "Bio Oil | 200mL" name, confirming the
     min-height 2-line reservation still works.
  E. Nothing else on the card moved — I can say this with confidence
     from the diff itself rather than a full-card screenshot: the only
     CSS touched was .product-card__name and the now-deleted
     .product-card__name-line rule, and the only TSX touched was the
     name-rendering piece of ProductCard.tsx. Price, save badge, cart
     button and stock dot markup and CSS are byte-for-byte unchanged.

Screenshots at 320px and 375px (dark theme, since that's this machine's
system default) visually confirm the same thing the DOM measurements
say: whole words only, ellipsis exactly where expected, consistent card
height across the row.

WAS THE SPLITTER'S MEASUREMENT ACTUALLY WRONG, AND WHY?
I could not find an outright logic bug in textWrap.ts's own measurement
code when I read it closely — the canvas ctx.font string composition and
ctx.measureText calls looked like the standard, normally-correct way to
do this. My best explanation for the symptom described (breaking a word
early despite room to spare) is a font-loading race: Plus Jakarta Sans is
a fairly narrow/compact typeface, and browsers substitute a WIDER
fallback font before it loads. If the very first measurement ran against
that fallback's metrics, it would have overestimated how much space each
word needed — for exactly the effect described (breaking early, leaving
unused space). Batch 6 added a document.fonts.ready.then(measure) re-run
specifically to fix this, but at the very narrow real column widths this
batch measured (76–122px, not the ~139px assumed), even a small
per-character overestimate from a fallback font is enough to flip a
"fits" decision to "doesn't fit" on a 12+ character word like
"Moisturising" — so if that re-measure didn't fire for any reason (a
component remount racing the promise, for instance), the wrong split
would stick. I can't confirm this was THE cause with certainty from a
local build, but it doesn't matter for the fix: removing the JS
measurement removes this entire class of bug regardless of which exact
mechanism caused it, because there's no longer a second measurement that
can disagree with the one the browser uses to paint.

PART 3 — Test added

There was no test runner configured at all (no test script in
package.json). I added Vitest, since it's the natural fit for a Vite
project and needed very little setup — one thing to flag: the newest
Vitest (v5) requires Vite 6/7/8, but this project is on Vite 5.4.8, so
I installed vitest@^2 (2.1.9) instead, which is compatible. Also added
jsdom as the test DOM environment.

Added:
  - vitest.config.ts (environment: jsdom, one setup file)
  - src/test/setup.ts (one line: sets IS_REACT_ACT_ENVIRONMENT so
    React's act() doesn't print a warning in tests)
  - src/components/viewer/ProductCard.test.tsx — renders the new
    ProductCardName component for each of the 8 real catalogue names
    from Part 2 and asserts the rendered element's textContent is
    EXACTLY equal to the original name, character for character. This
    is the right regression guard for what actually broke five times:
    native CSS wrapping never touches the DOM text at all (the browser
    only changes where it visually breaks the line, not the text
    content), so if a future session reintroduces a JS splitter that
    mutates the text into two truncated/broken fragments, this test
    fails immediately instead of waiting for another live-site report.
  - "test": "vitest run" added to package.json scripts.

`npm test` (or `npx vitest run`) → 9/9 tests pass.

--------------------------------------------------------------------
2. SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------

None needed. This batch touched no database schema.

--------------------------------------------------------------------
3. WHAT I COULD NOT FINISH
--------------------------------------------------------------------

Nothing from the task was skipped. One limit worth repeating (carried
forward from Batch 6, not new to this session): I cannot watch this
session's own new code running at https://naeems-all.vercel.app in
this same session, because only `main` deploys there and I'm not
allowed to push to `main`. Everything above — including the width
sweep — was verified against a local production build of this
branch, using the real compiled CSS/JS and real Supabase product data
fetched over the network, but served from localhost, not from the
live production URL. Please do the 30-second real-phone check once
this is merged, the same way Batch 6 asked.

Separately, and specific to this session: real browser window
resizing (the most direct way to test responsive widths) was not
available in this environment — Chrome's window would not shrink
below ~523px wide no matter what width I asked for. I used a
same-CSS, same-font, same-DOM-structure harness page with an
explicitly-sized wrapper div instead, and cross-checked its numbers
against the real live app's actual DOM before trusting it (see Part 2
above). I'm flagging this explicitly so it's not mistaken for having
tested with a real narrow window.

--------------------------------------------------------------------
4. WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------

- The task brief's own reference number for the name element's
  available width (~139px) turned out to be the card's width, not the
  name column's — see Part 2 above for the real numbers (76px–122px
  across 320–414px screens). Not a bug, just worth knowing for next
  time.
- No stray break-all / overflow-wrap:anywhere rules were hiding
  anywhere for the product card name — the only word-break rule left
  in the whole stylesheet is on a completely unrelated admin list
  component (.admin-wholesaler-row__meta), which I left untouched.

--------------------------------------------------------------------
5. DESIGN DECISIONS
--------------------------------------------------------------------

- Kept the name as its own tiny exported component (ProductCardName)
  rather than inlining the <h3> into ProductCard, purely so Part 3
  could test it in isolation without mocking Supabase/router context.
  It has zero behaviour beyond rendering the string — this is not a
  reintroduction of any measurement logic.
- Used the exact -webkit-line-clamp:2 pattern already used in three
  other places in this codebase (hero banner, bento tiles, review
  admin) rather than inventing a different two-line-clamp technique,
  per the task's own instruction to reuse whatever the codebase
  already does.
- Deleted the throwaway test harness (dist/wrap-test.html) and stopped
  the local static server I used to serve it before finishing — it was
  only ever a verification tool, not part of the app, and isn't in the
  commit.

--------------------------------------------------------------------
6. INDUSTRY-LEVEL SUGGESTIONS (not implemented — for you to schedule)
--------------------------------------------------------------------

1. [Trust signal — small, carried forward from Batches 5 and 6] Still
   true: product card prices are orange (#FF7A45), not near-black,
   against CLAUDE.md's own colour rule (src/styles/app.css, the
   .product-card__price rule). Not touched this batch — still out of
   scope — flagging again since it's now been carried forward three
   sessions running.
2. [Testing — small, now partially addressed] This batch added the
   first automated test in the project (Vitest + one test file for the
   card name). Now that the runner exists with almost no setup cost,
   it would be worth adding a couple more targeted tests the next time
   any of these recur: pricing/discount display logic
   (getDisplayPrice), stock status (isOutOfStock), and promo code
   validation are all pure-ish functions that have caused real bugs
   across past batches and would be cheap to cover now that the
   scaffolding exists.
3. [Performance — small] `npm run build` still warns that
   dist/assets/index-*.js is 700KB+ (194KB gzipped) — Vite's own
   suggestion is code-splitting with dynamic import(). Not urgent on
   its own, but worth watching as the product catalogue and feature
   set keep growing, especially given the target device is a
   mid-range Android phone on a slow connection.

====================================================================

====================================================================
BATCH 8 — Product Card Title Width (Root Cause)
Branch: fix/batch-8-title-width-root-cause
====================================================================

1. ROOT CAUSE
The whole app is deliberately capped to a max-width of 480px (see
.viewer-shell in app.css) so it always looks like a phone, even on a wide
desktop screen. But the product grid's "switch to 3 columns" rule fired at
the SAME number: `@media (min-width: 480px)`. That media query checks the
device's real physical viewport width, not the 480px-capped content area.
So the instant a real phone's CSS viewport width touched or passed 480px
(this genuinely happens on some large-screen Android phones — it depends on
the phone's display-size/zoom setting, which is why two different real
phones gave two different results for the same product), the grid crammed
a 3rd column into the exact same ~448px space that used to hold 2 columns.
Measured on the real production DOM: the title box for "CeraVe Foaming
Cleanser..." was 145–148px wide at 2 columns, and dropped to 77.5px the
moment 3 columns kicked in — a 47% cut. That starved width, not the
wrapping CSS from Batch 7, is why words wrapped early and left empty space
on the right. Confirmed with getBoundingClientRect() measurements at every
width from 320px to 700px, and cross-checked that the live production CSS
bundle matches source exactly (no build/minification discrepancy).

2. FIX
src/styles/app.css, line ~624: changed the product grid's breakpoint from
`@media (min-width: 480px)` to `@media (min-width: 640px)`, with a comment
explaining why. This keeps every realistic phone width at 2 columns (matching
CLAUDE.md's "two columns on mobile" spec) and only allows 3 columns on
genuinely wider screens (tablets/desktop) where the shell can't be confused
with a phone. No wrapping CSS, cart button, price, or badge code was touched.

3. BUILD
`npm run build` — PASSED, zero errors.

4. REAL-WIDTH TESTING
Tested all 104 real product names (not just 8) at 320/360/375/390/414px —
zero mid-word breaks found. Verified the specific 480px boundary directly:
before the fix, crossing 480px cut the title box width nearly in half;
after the fix, the grid stays 2-column all the way to 640px, and the
"CeraVe Foaming Cleanser..." title now fills its line almost edge-to-edge
at every real phone width tested.

5. VERIFICATION METHOD
Chrome's device toolbar/window resize was not controllable in this
environment (the automation window would not resize below desktop size), so
I verified using same-origin iframes pinned to exact CSS pixel widths
(320–700px) — this gives a real, independently-computed browser layout at
that exact width, not a simulation. The root-cause measurements (Part 1)
were taken directly against the live production DOM at
https://naeems-all.vercel.app. The fix verification (Parts 3–4) was run
against a local `npm run build` output served on localhost, since that is
the only way to test code that isn't merged yet. I could not test on a
physical phone from this environment.

====================================================================
BATCH 9 — Merge the Two Product Editors Into One
Branch: fix/batch-9-merge-product-editors
====================================================================

1. AUDIT — what the two editors looked like before this batch

  ProductEditSheet.tsx (card "..." menu, BottomSheet)  vs  ProductForm.tsx (/admin Products tab, Modal)

  Field/feature          | Card sheet          | Admin panel         | Notes
  ------------------------|---------------------|---------------------|------
  Name/brand/category     | yes                 | yes                 | same
  Description/how-to-use/ | yes                 | yes                 | same
    key ingredients/video |                     |                     |
  Retail/offer/wholesale  | yes                 | yes                 | same
  Stock quantity          | yes                 | yes                 | same
  Stock status (manual)   | NO — derived only   | yes (select, only   | admin-only capability
                           |                     | active w/o qty)     |
  Note/region/size        | yes                 | yes (region/size    | same
                           |                     | edit-mode only)     |
  Skin type/condition      | yes                 | yes                 | same (already shared)
  Variants                | yes (VariantEditor) | yes (VariantEditor) | already the same shared component
  Featured toggle         | yes                 | yes                 | same
  Active toggle           | NO                  | yes                 | admin-only capability
  Category creation       | NO                  | yes ("+ Create      | admin-only capability
                           |                     | new category")      |
  SKU display              | NO                  | yes (edit mode)     | admin-only capability
  Create mode (Add        | NO (edit only)      | yes                 | expected — no "add" from a card
    Product)               |                     |                     | that doesn't exist yet
  IMAGES                   | ImageStrip: paste a | ImageUploader: real | THE reported gap — confirmed
                           | URL by hand, one at | file upload (camera/| real, not assumed. Card sheet had
                           | a time. Drag-reorder| gallery), multiple  | NO way to add a photo from a phone,
                           | worked.              | at once, resize,    | only pasting an already-hosted
                           |                     | drag-reorder.       | image URL.

  Confirms Naeem's report: the card-edit flow could NOT add real photos —
  only a manually-typed image URL. The /admin panel's ImageUploader was
  already the complete one (file picker, drag&drop, multi-select, resize
  before upload, drag-to-reorder — reorder was fixed for it back in an
  earlier batch).

2. THE FIX — one shared editor

  Picked ProductForm.tsx's fields/logic as the base, NOT the card sheet —
  despite the card sheet being the one earlier sessions kept extending, the
  audit above shows the admin panel was actually the more complete
  implementation on every point of difference (Active toggle, category
  creation, SKU display, manual stock status, real image upload, create
  mode). The card sheet's only asset was its BottomSheet presentation,
  which was kept as-is.

  New file: src/components/admin/ProductEditorForm.tsx — owns every field,
  validation, image upload/resize, and the single save (insert or update).
  Both old files are now thin wrappers with no form markup or save logic
  of their own:
    - ProductForm.tsx        → Modal + <ProductEditorForm footerVariant="modal">
    - ProductEditSheet.tsx   → BottomSheet + <ProductEditorForm footerVariant="sheet">
  ("footerVariant" only changes which button row renders — Cancel+Save for
  the sheet to match every other sheet in the app, a single Save/Add button
  for the modal, matching its own existing pattern with the Modal's own X
  close button. No other behavioural difference between the two.)

  Moved into the shared form (now available from BOTH entry points):
    - Real multi-image upload via ImageUploader (fixes the reported gap)
    - Active toggle
    - SKU read-only display
    - "+ Create new category" inline
    - Manual Stock Status select

  Deleted ImageStrip.tsx (the old URL-paste image list) and its ~110 lines
  of now-dead CSS in app.css — nothing references it anymore.

  No database schema changes. No new fields were invented — this is a
  consolidation of what already existed in one editor or the other.

3. VERIFICATION — live, against the real production Supabase data

  Tested with two real products via the browser: "Beauty of Joseon Red
  Bean Water Gel" (has a real HC-0366 SKU, 5 images, wholesale price, no
  variants at the time of testing) and "Bio Oil | 200mL" (single image,
  Out of Stock status).

  - Both entry points load every field with the product's real current
    data (not blank/default): PASS. Checked SKU, name, prices, category,
    5 images with correct order/cover, skin tags, region/size.
  - Multi-image upload from BOTH entry points: PASS. Uploaded a real file
    through the admin panel's picker — became a 6th image, saved,
    reloaded, still there — then removed it and re-saved to restore the
    product exactly as it was. Confirmed the same ImageUploader (file
    picker, not URL paste) is now present in the card-edit sheet too.
  - Variants still work from both entry points: PASS. Added a temporary
    "TEST / 100ml / ৳999" variant from the card-edit sheet, saved — it
    appeared correctly in the /admin panel's editor for the SAME product
    (proving both go through the same variants table), then deleted it
    from the admin side to restore the product.
  - Active toggle round-trip: PASS. Toggled Bio Oil to Inactive from the
    admin panel, saved, confirmed the list showed "inactive" — then
    reopened and toggled it back to Active, saved, confirmed restored.
  - Saving from one entry point and reopening from the other shows the
    change: PASS — both the variant test and the image test above proved
    this directly (created/changed in one, visible in the other).
  - Both products were left in their original state when I finished —
    verified by reopening each one last time.

  Everything above was done live against the real database (not a test
  copy), with each temporary test change (image, variant, active toggle)
  undone immediately after it proved the round trip worked.

4. BUILD
`npm run build` — PASSED, zero errors, both before and after the live
verification.

5. WHAT I FOUND ALONG THE WAY
A recurring cosmetic glitch in this test environment's screenshot tool
(pages rendered solid black in several captures) had nothing to do with
the app — confirmed by reading the live DOM/accessibility tree instead,
which showed correct data throughout. Not something to act on.

====================================================================
BATCH 10 — Colour Cleanup, Variant Fixes, Bento Image, Category
Filter Layout, Cart Icon
Branch: fix/batch-10-cleanup-and-variants
====================================================================

PART 1 — Card price colour
Fixed. `.product-card__price` and `.bento-tile__price` were still
`#FF7A45` (orange) — CLAUDE.md says prices must be near-black. Changed
both to `var(--color-text-primary)`, the exact treatment already used on
checkout (`.checkout-cart__line-total` etc). Left the strikethrough price
and "Save X%" badge alone — CLAUDE.md's rule only names prices. Verified
live: an in-stock card's price now renders white (this app's dark-theme
"near-black" equivalent) instead of orange.
Files: src/styles/app.css

PART 2 — Banner "Background colour" does nothing
Removed the field rather than wiring it up. HeroBanner's no-photo slide
already uses a theme-aware CSS variable (`--banner-bg`, different value
per light/dark mode); a single admin-picked hex per slide has no dark-mode
counterpart and would fight that system, plus risks off-brand colours.
Removed `background_color` from the BannerSlide type, bannerSlides.ts, and
the colour picker UI in BannerEditSheet.tsx. Also found and fixed a SECOND,
apparently-duplicate banner editor living in SettingsTab.tsx
(BannerSlidesPanel) that referenced the same field and both write to the
same app_settings row as BannerEditSheet — see part 4 below, this looks
like real duplication worth knowing about, not something I restructured.
Files: src/types/index.ts, src/lib/bannerSlides.ts,
src/components/admin/edit-sheets/BannerEditSheet.tsx,
src/components/admin/SettingsTab.tsx

PART 3A — Edit Mode doesn't survive reload
Root cause: AdminEditContext held the on/off toggle in plain useState,
deliberately reset on every mount. Persisted it to localStorage instead —
kept the deliberate on/off toggle (matches the rest of the app's pattern)
rather than switching to "always on for admins". Still force-cleared the
moment the session isn't a real approved admin, so it can never leak to a
signed-out visitor or a different account on the same device. First pass
had a real bug of its own: the clear-on-non-admin effect fired during the
one-render window where auth is still resolving (isAdmin reads false
before the session check completes), wiping the stored value before it
ever got a chance. Fixed by also gating on authLoading. Verified live:
turned Edit Mode on, did a real page reload, confirmed it was still on
(and the orange top bar was back) — twice, once before the authLoading
fix (failed, caught it) and once after (passed).
Files: src/contexts/AdminEditContext.tsx

PART 3B — Remove Copy Price from product detail page
Removed the button, its `copied` state/timer, and the now-unused
buildCopyText/copyToClipboard imports from ProductDetailPage.tsx. Kept
lib/clipboard.ts itself and the card menu's own "Copy Price" item —
CardMenu.tsx uses the same two functions and was explicitly out of scope.
Only "Add to Cart" remains on the detail page now; simplified the
now-single-button wrapper and removed the CSS that only existed to lay
out two buttons side by side.
Files: src/pages/ProductDetailPage.tsx, src/styles/app.css

PART 4 — Variants
4A. Duplicate variant display: the earlier case-normalization fix only
touched variant ROWS (VariantEditor already uppercases Region on save).
It never touched the product's OWN Region field — set in
ProductEditorForm.tsx's plain text field, or CombineProductsSheet.tsx's
"shared content source" label — which is option zero in the picker
(variantOptionsFor). A product whose own Region was typed "au" while its
real variant rows were saved as "AU" showed two chips for what's really
one Region — that's the duplicate, a real stored-data mismatch, not a
display bug. Uppercased both save sites to match VariantEditor. Wrote
migration-018 to fix existing rows (casing only, no value/row changes) —
see SQL section below. I don't have a live product with an existing
mismatch to demo the before/after on, but the mechanism is unambiguous
from the code and the fix directly closes it.

4B. First-tap "shake": read every relevant CSS transition (all already
correctly scoped to transform/opacity/colour, no width/height/position
animated) and instrumented the actual DOM through a real variant tap
(getBoundingClientRect on the gallery, price, note, actions and selector,
sampled every 50ms for 500ms) — found zero geometry change on the two
real-variant products in the live catalog, so I could not reproduce a
layout shift with measurement. Found and fixed a real, separate gap that
matches the symptom well and is a well-known cause of exactly this kind
of first-tap jank: no `<button>` on the whole site had
`touch-action: manipulation`, so a mobile browser arms its double-tap-to-
zoom gesture detector on every tap and only resolves it after ~300ms —
visibly nudging the page on that first tap. Added it to the global button
reset. I can't fully confirm this was THE mechanism Naeem saw (no way to
test real mobile Safari/Chrome touch behaviour from here), so flagging
that honestly rather than claiming a confirmed fix.

4C. Guided Region/Size: replaced the two free-text inputs in the variant
Add/Edit sub-form with GuidedField (new, in SheetChrome.tsx) — a select of
every Region/Size already used anywhere in the catalog (product base
labels + all variant rows, via new distinctRegions/distinctSizes in
lib/variants.ts and a new `allVariants` on ProductContext), with a
"+ Add new…" option that drops to free text for a genuinely new value.
Built on Batch 9's merged ProductEditorForm/VariantEditor, so this reaches
both entry points (card sheet and /admin) automatically. This directly
prevents 4A's root cause recurring for new variants.

4D. Variant image sorting: previously the gallery only ever showed the
SELECTED variant's own photo swapped into slot 1 — a different variant's
photo was invisible whenever it wasn't the active selection. Added
mergedGalleryImages()/variantImagesBySize() (lib/variants.ts): every
variant's own photo, smallest Size first (parsed from the leading number
in the Size string), merged with the product's own gallery, deduplicated.
Selecting an option still moves its own photo to slot 1 — the rest keeps
this same smallest-to-largest order, so nothing drops out of the gallery
anymore.
Files: src/lib/variants.ts, src/components/admin/edit-sheets/VariantEditor.tsx,
src/components/admin/edit-sheets/SheetChrome.tsx,
src/components/admin/CombineProductsSheet.tsx,
src/components/admin/ProductEditorForm.tsx, src/contexts/ProductContext.tsx,
src/pages/ProductDetailPage.tsx

PART 5 — WhatsApp / PDF variant display
Verified, not a bug. buildOrderWhatsAppText and buildOrderPdf
(lib/orderExport.ts) both already append the variant label in parentheses
after the product name — e.g. "CeraVe Hydrating Cleanser (473) x1 -
৳1,650" on WhatsApp and the same line format in the PDF. The label is
snapshotted onto the cart line at add-time (CartContext) specifically so
it survives even if the variant is later deleted. Nothing changed here.

PART 6 — Bento tile images stay blank
Confirmed root cause: the left (swipe stack) and right-top (flip) bento
tiles never rendered a product image at all — no <img>, no
background-image, ever, for any product, dragged in or not. Only the
bottom carousel's custom link tiles (a completely separate admin form)
ever showed a photo. Added a background-image (the product's real cover
photo, same CSS scrim-over-photo treatment as the custom tiles) to both
tile types. Verified live: a real product's actual Supabase-hosted photo
now renders as the tile's background.
Files: src/components/viewer/BentoGrid.tsx, src/styles/app.css

PART 7 — Category chip layout breaks on some categories
Could not visually reproduce the cut-off/overlap in this environment
despite substantial live testing (instrumented before/after geometry of
the chip row, grid and every home section across the full transition,
0-2000ms, both from the top of the page and mid-scroll; switching "All"
(104 products) to Body Care (13) and back) — every measurement came back
clean, no overlap, no stale height. I did find and fix one genuine race
condition on the way: `scrollToProducts()` ran synchronously right after
`setSelectedCategoryId()`, so it measured the OLD (pre-filter) layout
before React had re-rendered the new, often very differently-sized grid —
exactly the kind of scroll-vs-reflow race that would show up worse the
bigger the size difference between categories (Body Care vs Eye Care).
Deferred it two animation frames so it always measures the real, final
layout. This is a genuine bug fix, but I can't promise it's THE bug Naeem
saw — reporting that honestly rather than claiming a confirmed fix.
Files: src/pages/ViewerPage.tsx

PART 8 — Cart button checkmark reverts after ~1 second
Root cause: the checkmark was driven by a local `justAdded` flag on a
500ms timeout, reverting to the plain icon regardless of whether the item
was still genuinely in the cart — which is exactly what read as "the tap
didn't work". Changed it to derive directly from real cart membership
(`items.some(i => i.productId === productId)` from CartContext) instead
of a timer, so the icon can never contradict the actual cart state; it
only ever shows the plain icon when the product truly isn't in the cart.
Kept the decorative tap-pulse ring as its own state (unrelated to the
icon). Verified live: added an item, checked the button's class at 0ms,
200ms, 600ms, 1200ms and 2000ms — checkmark held throughout.
Files: src/components/viewer/CartButton.tsx

--------------------------------------------------------------------
SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
migration-018-normalize-variant-region-case.sql — fixes any existing
product/variant rows where Region casing doesn't match (Part 4A). Casing
only, no values or rows change, safe to re-run:

UPDATE products
SET region = UPPER(region)
WHERE region IS NOT NULL AND region <> UPPER(region);

UPDATE product_variants
SET region = UPPER(region)
WHERE region <> UPPER(region);

--------------------------------------------------------------------
WHAT I FOUND ALONG THE WAY
--------------------------------------------------------------------
- SettingsTab.tsx has a full second banner-slide editor
  (BannerSlidesPanel) alongside BannerEditSheet.tsx, both writing the same
  app_settings row — editing in one place then the other risks silently
  clobbering whichever was saved last. Didn't touch beyond the
  Part 2 field removal needed to keep both compiling; a real fix would be
  picking one and deleting the other, which is a bigger, separate task.

Build: `npm run build` — PASSED, zero errors. `npx vitest run` — 9/9
tests passed.

========================================================================
BATCH 12 — Desktop image upload: variant drag-and-drop, main row overflow

PART 1 — Variant image drag-and-drop didn't work
Root cause: the variant "Image (optional)" field uses a different, older
component (AdminImagePicker) than the main product's images (ImageUploader)
— and AdminImagePicker had no drag-and-drop code at all, just the "Upload
image" button/file-picker. Dropping a file onto it did nothing (confirmed
live, before/after: no dragover class, no preventDefault, no upload
attempt at all before the fix).
Fix: added the same dragover/dragleave/drop handling ImageUploader already
has to AdminImagePicker (drop onto the image preview box), reusing its
existing upload/validation logic. Since AdminImagePicker is also the same
component behind category images, banner slides, and the expert photo,
those all gained working drag-and-drop too as a side effect of the shared
component — did not touch the button/file-picker path.
Verified live (real drag events dispatched on the actual component, both
before and after the fix): before — no reaction at all; after — the drop
zone highlights on drag-over and the drop correctly reaches the upload
code path. PASSED.
Files: src/components/admin/AdminImagePicker.tsx

PART 2 — Main image row shifts right, can't scroll back
Root cause: the thumbnail row (ImageUploader) is a deliberate single
scrolling row (not a wrapping grid — wrapping would break the hold-to-drag
reorder, which only knows how to resolve position along one horizontal
axis). It already had `overflow-x: auto`, so it WAS technically scrollable
— but its scrollbar was deliberately hidden (`scrollbar-width: none` +
`-webkit-scrollbar{display:none}`) for a clean mobile look, which also
removed the only affordance/handle a desktop mouse user had once the row
overflowed. A plain vertical mouse wheel does nothing to a horizontal row
by default either, so once enough images were added to overflow, a
desktop user was stuck — exactly what Naeem saw.
Fix (kept the single-row design, didn't switch to wrapping, to avoid
breaking the reorder drag): swapped the hidden scrollbar for a thin,
visible one (so there's now a real handle to drag), and added a wheel
handler that redirects vertical mouse-wheel scrolling into horizontal
scroll. The row is also keyboard-scrollable now (made it focusable) —
confirmed Left/Right arrow keys scroll it once focused.
Image count limit: checked the upload code and the database column
(`products.image_urls`, a plain Postgres text array) — there is no cap
anywhere, client or database. Effectively unlimited, as Naeem suspected.
Verified live (real overflow row, 8 images): before the fix, a vertical
wheel scroll did nothing; after the fix, the same scroll moved the row
(scrollLeft 0 → 333px), and arrow keys scrolled it back. PASSED.
Files: src/components/admin/ImageUploader.tsx, src/styles/app.css

PART 3 — Mobile regression check
Both fixes are desktop-only in effect: HTML5 drag events never fire from
a touch gesture (nothing new to trigger on mobile), and the wheel handler
only responds to `wheel` events, which touch scrolling doesn't send —
mobile still scrolls the row by native swipe exactly as before. The
existing hold-to-drag reorder logic (useDragReorder) wasn't touched.
Not re-tested live on an actual touch device this session — reasoning
from the code is that neither change has any mobile code path to affect.
BATCH 11 — Variant tap slide, SIZE label, Region dropdown + management

PART 1-2 — Variant tap "full screen slide"
Root cause found and confirmed live (not a guess this time). Tapping a
size/region chip calls setSearchParams(..., {replace:true}) to write
?variant=<id> into the URL. React Router's setSearchParams never carries
over the current location's `state` — so the FIRST such tap after landing
via the card's hero-transition silently wiped state.hero (which was
telling PageTransition.tsx to skip its own slide because the native View
Transition was already animating that arrival). Losing that flag flipped
PageTransition's slide class from absent to present on the SAME still-
mounted wrapper div (no remount — key is the pathname, unchanged), and
adding a fresh animation class to an existing element replays the CSS
animation from 0 — a real, once-only, full-screen slide-in. Every tap
after the first has nothing left to lose (state was already gone), so it
never repeats. Matches Naeem's description exactly.

Fix: PageTransition now only plays the slide when the PATHNAME actually
changed (tracked in a ref, compared each render) — a same-page update
like ?variant=<id> can never trigger it again, no matter what happens to
location.state.

Verified live (real browser, real product data, both the dev server and
a built production bundle): attached a MutationObserver and confirmed
the slide class WAS added on the first variant tap before the fix, was
NOT added on the first OR second tap after the fix, and that a real
page-to-page navigation (Back button) still plays its slide correctly in
the production build — PASSED on 2 products (Beauty of Joseon Revive Eye
Serum, CeraVe Hydrating Cleanser).
Note: in the Vite DEV server only (not production), React 18 StrictMode's
double-render briefly appeared to suppress the Back slide too, because
the fix's pathname-tracking ref gets touched twice per render pass under
StrictMode. Confirmed this does NOT happen in the actual production
build (which never double-renders) — flagging only so dev-mode testing
in a future session isn't confusing.

SIZE label: already renders correctly, in code and in the live DOM
(confirmed visible with text "Size") on the exact product from your
screenshot (Beauty of Joseon, 30ml/10ml). It's small (9.5px) and light
gray by design, which likely made it easy to miss in a compressed
screenshot. No bug found — left as is.
Files: src/components/shared/PageTransition.tsx

PART 5 — Region: product's own field was still free text
5A. The product's own Region field (ProductEditorForm.tsx) now uses the
same GuidedField dropdown VariantEditor already uses for variant rows,
sourced from the same distinctRegions() list — a product's own Region
and its variants' Region can no longer drift apart by typo/casing going
forward. (Size stays free text for the product's own field, as scoped.)

5B. Added a Region/Size management view in the admin Categories tab,
right under Categories — same kind of catalog-wide label. Lists every
Region/Size in use with a usage count, lets Naeem rename one (updates
every product and variant row carrying that exact value, one operation),
and has a Delete button that's blocked with the usage count whenever
anything still uses it. Honest caveat: Region/Size aren't their own
database rows — they only appear in this list because something already
references them — so Delete will in practice always show the "in use"
message; it's a safety guard, not a normal path to removing one.
"+ Add new" isn't here — GuidedField's own "+ Add new…" already covers
that from both the product and variant forms.

5C. migration-019 (below) trims + uppercases both products.region and
product_variants.region so a product's own Region always matches its
variant rows' Region exactly, even where they only differed by stray
whitespace or case. Also re-runs migration-018's UPPER() in case that one
was never applied. Casing/whitespace only — never invents or merges
Region names.

5D. Checked every product/variant pair in the live catalog — no existing
casing/whitespace mismatch was found right now, so there's nothing
currently mis-displaying to demo a before/after on. Did confirm live that
the grouping logic itself is correct: Beauty of Joseon's own Region
("KOREA") already matches its variant's Region exactly, and the detail
page correctly shows ONE Region (no Region row at all, since there's
only one) with both sizes grouped under it — the exact behaviour this
migration protects.
Could not verify 5A/5B by actually logging into /admin — this
environment has no admin credentials. Verified by code review (matches
the existing VariantEditor pattern exactly) and a clean `npm run build`.
Files: src/components/admin/ProductEditorForm.tsx,
src/components/admin/RegionSizeManager.tsx (new), src/pages/AdminPage.tsx,
src/styles/app.css

--------------------------------------------------------------------
SQL NAEEM MUST RUN IN SUPABASE
--------------------------------------------------------------------
migration-019-normalize-region-whitespace.sql — trims + uppercases
products.region and product_variants.region so a product's own Region
always matches its variants' Region exactly. Safe to re-run:

UPDATE products
SET region = UPPER(TRIM(region))
WHERE region IS NOT NULL AND region <> UPPER(TRIM(region));

UPDATE product_variants
SET region = UPPER(TRIM(region))
WHERE region <> UPPER(TRIM(region));

(Covers migration-018 too, if that one was never run.)

Build: `npm run build` — PASSED, zero errors. `npx vitest run` — 9/9
tests passed.
